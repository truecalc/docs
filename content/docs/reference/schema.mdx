---
title: Workbook JSON schema
description: The workbook JSON schema v1 — the cross-surface contract shared by Rust, WASM, MCP, and the hosted API.
---

Stub — this page will publish the workbook JSON Schema v1 (canonical
serialization per RFC 8785/JCS, required `engine` and `version` fields,
authored-vs-spilled cell markers) once `truecalc-workbook` ships.
