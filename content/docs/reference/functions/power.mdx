---
title: "POWER"
description: "Number raised to a power"
seoTitle: "POWER: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `POWER` — conformant with Google Sheets and Excel. Number raised to a power"
llmsDescription: "TrueCalc POWER function: Number raised to a power Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use POWER instead of the ^ operator?","answer":"They compute the same result; POWER(base, exponent) is often easier to read inside long formulas, while ^ is more compact for quick exponents like squares."},{"question":"How do I take a root with POWER?","answer":"Raise the value to a fractional exponent. POWER(27, 1/3) returns the cube root, and POWER(x, 0.5) is the square root."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=POWER(2, 10)"} />

## When to use it

### When should I use POWER instead of the ^ operator?

They compute the same result; POWER(base, exponent) is often easier to read inside long formulas, while ^ is more compact for quick exponents like squares.

### How do I take a root with POWER?

Raise the value to a fractional exponent. POWER(27, 1/3) returns the cube root, and POWER(x, 0.5) is the square root.

## Use cases

**Square or cube a value**

<TryIt formula={"=POWER(2, 10)"} />

**Compound a growth rate over many periods**

<TryIt formula={"=POWER(1.05, 30)"} />

**Take the cube root via a fractional exponent**

<TryIt formula={"=POWER(1000000, 1/3)"} />

## Used in

[finance](/industries/finance) · [personal-finance](/industries/personal-finance) · [science](/industries/science) · [engineering](/industries/engineering)

## Syntax

```
POWER(number, power)
```

## Examples

```formula
=POWER(2,4) // => 16
=POWER(3,1.5) // => 5.196152422706632
=POWER(0,10) // => 0
```

