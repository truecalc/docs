---
title: "FLATTEN"
description: "Flattens an array into a single column"
seoTitle: "FLATTEN: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `FLATTEN` — conformant with Google Sheets and Excel. Flattens an array into a single column"
llmsDescription: "TrueCalc FLATTEN function: Flattens an array into a single column Google Sheets/Excel conformant with fixture-verified behavior."
category: "array"
jsonLdFaq: [{"question":"When should I use FLATTEN?","answer":"Use FLATTEN to collapse a multi-dimensional array into a single column, useful before passing data to functions that expect a flat list."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=FLATTEN({1,2;3,4})"} />

## When to use it

### When should I use FLATTEN?

Use FLATTEN to collapse a multi-dimensional array into a single column, useful before passing data to functions that expect a flat list.

## Used in

[operations](/industries/operations) · [science](/industries/science)

## Syntax

```
FLATTEN(array)
```

## Examples

```formula
=FLATTEN({1,2;3,4}) // => 1
=FLATTEN({1,2,3}) // => 1
=FLATTEN({42}) // => 42
```

