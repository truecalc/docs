---
title: "COUNTIF"
description: "Count cells matching criterion"
seoTitle: "COUNTIF: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `COUNTIF` — conformant with Google Sheets and Excel. Count cells matching criterion"
llmsDescription: "TrueCalc COUNTIF function: Count cells matching criterion Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use COUNTIF instead of SUMIF?","answer":"Use COUNTIF to count how many cells meet a condition; use SUMIF to add up the values of the cells that meet a condition."},{"question":"How do I write the condition in COUNTIF?","answer":"Put the criteria in quotes, e.g. greater-than two as a quoted comparison, or a quoted word to count matching text."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=COUNTIF({1, 2, 3, 4}, \">2\")"} />

## When to use it

### When should I use COUNTIF instead of SUMIF?

Use COUNTIF to count how many cells meet a condition; use SUMIF to add up the values of the cells that meet a condition.

### How do I write the condition in COUNTIF?

Put the criteria in quotes, e.g. greater-than two as a quoted comparison, or a quoted word to count matching text.

## Use cases

**Count values greater than a threshold**

<TryIt formula={"=COUNTIF({1, 2, 3, 4}, \">2\")"} />

**Count how many entries match a label**

<TryIt formula={"=COUNTIF({\"Yes\", \"No\", \"Yes\", \"Yes\"}, \"Yes\")"} />

**Count scores at or above a passing mark**

<TryIt formula={"=COUNTIF({55, 62, 70, 48}, \">=60\")"} />

## Used in

[operations](/industries/operations) · [sales](/industries/sales) · [education](/industries/education) · [hr](/industries/hr)

## Syntax

```
COUNTIF(range, criterion)
```

## Examples

```formula
=COUNTIF({10,25,30,15,22},">20") // => 3
=COUNTIF({"Paid","Unpaid","Paid","Pending"},"Paid") // => 2
=COUNTIF({"apple","application","banana","apricot"},"ap*") // => 3
```

