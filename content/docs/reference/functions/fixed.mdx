---
title: "FIXED"
description: "Format number as text with fixed decimal places"
seoTitle: "FIXED: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `FIXED` — conformant with Google Sheets and Excel. Format number as text with fixed decimal places"
llmsDescription: "TrueCalc FIXED function: Format number as text with fixed decimal places Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use FIXED instead of TEXT?","answer":"Use FIXED when you need a number formatted as text with a specific number of decimal places and optional comma separators, without needing a full custom format string."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=FIXED(1234.5678, 2)"} />

## When to use it

### When should I use FIXED instead of TEXT?

Use FIXED when you need a number formatted as text with a specific number of decimal places and optional comma separators, without needing a full custom format string.

## Used in

[operations](/industries/operations)

## Syntax

```
FIXED(number, [decimals], [no_commas])
```

