---
title: "TZSTRING"
description: "Canonical RFC-9557 string for a zone-aware instant"
seoTitle: "TZSTRING: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZSTRING` — conformant with Google Sheets and Excel. Canonical RFC-9557 string for a zone-aware instant"
llmsDescription: "Use TZSTRING(zoned) to get the canonical RFC-9557 wire-format string for a zone-aware instant, e.g. \"2026-07-14T09:00:00-04:00[America/New_York]\". Use for storage, API payloads, and logging. Inverse of TZPARSE. For a human-readable label use TZTEXT i"
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZSTRING instead of TZTEXT?","answer":"Use TZSTRING when you need the full RFC-9557 machine-readable wire format (e.g., for API payloads, database storage, or log pipelines) that includes both the UTC offset and the IANA timezone name. Use TZTEXT when you need a human-readable display string."},{"question":"What does TZSTRING include that a plain ISO-8601 timestamp does not?","answer":"TZSTRING appends the IANA timezone name in brackets (e.g., [America/New_York]) after the UTC offset, making it an RFC-9557 zoned datetime. This lets any RFC-9557-aware parser reconstruct the exact civil time and timezone rules, not just the UTC instant."},{"question":"Can I round-trip a zoned value through TZSTRING and TZPARSE?","answer":"Yes. TZPARSE is the exact inverse of TZSTRING, so TZPARSE(TZSTRING(zoned)) recovers the original zone-aware instant without any loss of timezone information."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZSTRING(TZDATETIME(2026, 7, 14, 9, 0, 0, \"America/New_York\"))"} />

## When to use it

### When should I use TZSTRING instead of TZTEXT?

Use TZSTRING when you need the full RFC-9557 machine-readable wire format (e.g., for API payloads, database storage, or log pipelines) that includes both the UTC offset and the IANA timezone name. Use TZTEXT when you need a human-readable display string.

### What does TZSTRING include that a plain ISO-8601 timestamp does not?

TZSTRING appends the IANA timezone name in brackets (e.g., [America/New_York]) after the UTC offset, making it an RFC-9557 zoned datetime. This lets any RFC-9557-aware parser reconstruct the exact civil time and timezone rules, not just the UTC instant.

### Can I round-trip a zoned value through TZSTRING and TZPARSE?

Yes. TZPARSE is the exact inverse of TZSTRING, so TZPARSE(TZSTRING(zoned)) recovers the original zone-aware instant without any loss of timezone information.

## Use cases

**Log a converted instant in the destination timezone**

<TryIt formula={"=TZSTRING(TZCONVERT(TZDATETIME(2026, 7, 14, 14, 0, 0, \"UTC\"), \"Asia/Tokyo\"))"} />

**Store a shift start time as a portable zoned string**

<TryIt formula={"=TZSTRING(TZDATETIME(2026, 8, 1, 6, 30, 0, \"America/Chicago\"))"} />

## Used in

[engineering](/industries/engineering) · [operations](/industries/operations)

## Syntax

```
TZSTRING(zoned)
```

