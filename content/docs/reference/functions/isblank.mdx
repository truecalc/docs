---
title: "ISBLANK"
description: "True if value is blank"
seoTitle: "ISBLANK: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `ISBLANK` — conformant with Google Sheets and Excel. True if value is blank"
llmsDescription: "TrueCalc ISBLANK function: True if value is blank Google Sheets/Excel conformant with fixture-verified behavior."
category: "logical"
jsonLdFaq: [{"question":"When should I use ISBLANK?","answer":"Use ISBLANK to test whether a cell is truly empty, typically inside an IF so you can flag or skip rows that have no data yet."},{"question":"Is an empty string the same as blank to ISBLANK?","answer":"No — ISBLANK is strict: a cell holding an empty string (\"\") is not truly empty, so ISBLANK(\"\") returns FALSE. Use COUNTBLANK if you want empty strings counted as blank."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=ISBLANK(\"\")"} />

## When to use it

### When should I use ISBLANK?

Use ISBLANK to test whether a cell is truly empty, typically inside an IF so you can flag or skip rows that have no data yet.

### Is an empty string the same as blank to ISBLANK?

No — ISBLANK is strict: a cell holding an empty string ("") is not truly empty, so ISBLANK("") returns FALSE. Use COUNTBLANK if you want empty strings counted as blank.

## Use cases

**Text is never blank**

<TryIt formula={"=ISBLANK(\"text\")"} />

**Zero is a value, not a blank**

<TryIt formula={"=ISBLANK(0)"} />

## Used in

[operations](/industries/operations)

## Syntax

```
ISBLANK(value)
```

