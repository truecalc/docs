---
title: "OR"
description: "True if any argument is true"
seoTitle: "OR: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `OR` — conformant with Google Sheets and Excel. True if any argument is true"
llmsDescription: "TrueCalc OR function: True if any argument is true Google Sheets/Excel conformant with fixture-verified behavior."
category: "logical"
jsonLdFaq: [{"question":"When should I use OR instead of AND?","answer":"Use OR when any one condition being true is enough; use AND when every condition must be true."},{"question":"How do I combine OR with IF?","answer":"Put OR inside the IF test, so the IF returns its true result when at least one of the OR conditions holds."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=OR(FALSE, 1 > 0)"} />

## When to use it

### When should I use OR instead of AND?

Use OR when any one condition being true is enough; use AND when every condition must be true.

### How do I combine OR with IF?

Put OR inside the IF test, so the IF returns its true result when at least one of the OR conditions holds.

## Use cases

**Flag a value outside an allowed band**

<TryIt formula={"=OR(3 < 5, 3 > 10)"} />

**Approve when at least one rule passes**

<TryIt formula={"=IF(OR(4 > 5, 4 < 12), \"yes\", \"no\")"} />

## Used in

[operations](/industries/operations)

## Syntax

```
OR(value1,...)
```

