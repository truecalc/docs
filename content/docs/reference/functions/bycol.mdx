---
title: "BYCOL"
description: "Applies a LAMBDA to each column of an array"
seoTitle: "BYCOL: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `BYCOL` — conformant with Google Sheets and Excel. Applies a LAMBDA to each column of an array"
llmsDescription: "TrueCalc BYCOL function: Applies a LAMBDA to each column of an array Google Sheets/Excel conformant with fixture-verified behavior."
category: "array"
jsonLdFaq: [{"question":"When should I use BYCOL?","answer":"Use BYCOL to apply a LAMBDA function to each column of an array individually, returning a single aggregated result per column."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=BYCOL({1,2;3,4},LAMBDA(col,SUM(col)))"} />

## When to use it

### When should I use BYCOL?

Use BYCOL to apply a LAMBDA function to each column of an array individually, returning a single aggregated result per column.

## Used in

[operations](/industries/operations) · [science](/industries/science)

## Syntax

```
BYCOL(array, lambda)
```

