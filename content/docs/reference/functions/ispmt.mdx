---
title: "ISPMT"
description: "Interest paid for a period (straight line)"
seoTitle: "ISPMT: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `ISPMT` — conformant with Google Sheets and Excel. Interest paid for a period (straight line)"
llmsDescription: "TrueCalc ISPMT function: Interest paid for a period (straight line) Google Sheets/Excel conformant with fixture-verified behavior."
category: "financial"
jsonLdFaq: [{"question":"When should I use ISPMT instead of IPMT?","answer":"Use ISPMT for loans that use straight-line (equal-principal) amortisation — each period's interest is calculated on the outstanding balance after the period's principal repayment, unlike IPMT which uses standard annuity amortisation."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=ISPMT(0.15,2,5,1000)"} />

## When to use it

### When should I use ISPMT instead of IPMT?

Use ISPMT for loans that use straight-line (equal-principal) amortisation — each period's interest is calculated on the outstanding balance after the period's principal repayment, unlike IPMT which uses standard annuity amortisation.

## Use cases

**Interest paid in period 2 under straight-line amortisation**

<TryIt formula={"=ISPMT(0.15,2,5,1000)"} />

## Used in

[finance](/industries/finance) · [lending](/industries/lending)

## Syntax

```
ISPMT(rate, per, nper, pv)
```

## Examples

```formula
=ISPMT(0.15,2,5,1000) // => -90
```

