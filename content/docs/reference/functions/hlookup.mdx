---
title: "HLOOKUP"
description: "Searches first row of range, returns value from index row"
seoTitle: "HLOOKUP: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `HLOOKUP` — conformant with Google Sheets and Excel. Searches first row of range, returns value from index row"
llmsDescription: "TrueCalc HLOOKUP function: Searches first row of range, returns value from index row Google Sheets/Excel conformant with fixture-verified behavior."
category: "lookup"
jsonLdFaq: [{"question":"When should I use HLOOKUP instead of VLOOKUP?","answer":"Use HLOOKUP when your data is laid out in rows, so the lookup runs across the top row and pulls a value from a row below. Use VLOOKUP when the data is in columns."},{"question":"Why does my HLOOKUP return the wrong column?","answer":"Usually because the last argument defaults to approximate match. Pass FALSE as the fourth argument to force an exact match unless your top row is sorted and you want the nearest value."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=HLOOKUP(\"b\", {\"a\", \"b\", \"c\"; 1, 2, 3}, 2, FALSE)"} />

## When to use it

### When should I use HLOOKUP instead of VLOOKUP?

Use HLOOKUP when your data is laid out in rows, so the lookup runs across the top row and pulls a value from a row below. Use VLOOKUP when the data is in columns.

### Why does my HLOOKUP return the wrong column?

Usually because the last argument defaults to approximate match. Pass FALSE as the fourth argument to force an exact match unless your top row is sorted and you want the nearest value.

## Use cases

**Look up a price by product label across a row**

<TryIt formula={"=HLOOKUP(\"b\", {\"a\", \"b\", \"c\"; 1, 2, 3}, 2, FALSE)"} />

**Find a quarter's revenue from a row header**

```formula
=HLOOKUP("Q2", {"Q1", "Q2", "Q3"; 100, 250, 175}, 2, FALSE)
```

**Map a tier to its discount across a row**

<TryIt formula={"=HLOOKUP(2, {1, 2, 3; 0.05, 0.1, 0.15}, 2, FALSE)"} />

## Used in

[retail](/industries/retail) · [sales](/industries/sales) · [finance](/industries/finance) · [operations](/industries/operations)

## Syntax

```
HLOOKUP(search_key, range, index, [is_sorted])
```

## Examples

```formula
=HLOOKUP("b",{"a","b","c";1,2,3},2,FALSE) // => 2
=HLOOKUP("a",{"a","b","c";10,20,30},2,FALSE) // => 10
=HLOOKUP("c",{"a","b","c";10,20,30},2,FALSE) // => 30
```

