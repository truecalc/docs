---
title: "INT"
description: "Round down to nearest integer"
seoTitle: "INT: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `INT` — conformant with Google Sheets and Excel. Round down to nearest integer"
llmsDescription: "TrueCalc INT function: Round down to nearest integer Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use INT instead of ROUNDDOWN or TRUNC?","answer":"INT rounds down to the nearest whole number, so for negatives it moves away from zero (INT(-2.5) is -3). TRUNC and ROUNDDOWN simply drop the decimals toward zero (-2). Pick INT when you want a true mathematical floor."},{"question":"Can I use INT to extract the whole-dollar part of an amount?","answer":"Yes. INT(amount) gives the whole units, and amount minus INT(amount) gives the fractional remainder, which is handy for splitting currency into dollars and cents."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=INT(7.9)"} />

## When to use it

### When should I use INT instead of ROUNDDOWN or TRUNC?

INT rounds down to the nearest whole number, so for negatives it moves away from zero (INT(-2.5) is -3). TRUNC and ROUNDDOWN simply drop the decimals toward zero (-2). Pick INT when you want a true mathematical floor.

### Can I use INT to extract the whole-dollar part of an amount?

Yes. INT(amount) gives the whole units, and amount minus INT(amount) gives the fractional remainder, which is handy for splitting currency into dollars and cents.

## Use cases

**Floor a negative value away from zero**

<TryIt formula={"=INT(-2.5)"} />

**Whole-dollar portion of a price**

<TryIt formula={"=INT(1234.99)"} />

## Used in

[operations](/industries/operations)

## Syntax

```
INT(number)
```

## Examples

```formula
=INT(99.44) // => 99
=INT(-8.9) // => -9
=INT(7) // => 7
```

