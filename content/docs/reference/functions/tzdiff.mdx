---
title: "TZDIFF"
description: "Elapsed time from b to a on the absolute timeline (DST-immune)"
seoTitle: "TZDIFF: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZDIFF` — conformant with Google Sheets and Excel. Elapsed time from b to a on the absolute timeline (DST-immune)"
llmsDescription: "TrueCalc TZDIFF function: Elapsed time from b to a on the absolute timeline (DST-immune) Google Sheets/Excel conformant with fixture-verified behavior."
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZDIFF instead of subtracting two date serials?","answer":"Use TZDIFF when the two timestamps are in different time zones or span a DST transition. Serial subtraction counts wall-clock ticks and can be off by an hour; TZDIFF works on the absolute UTC timeline so it always returns real elapsed time."},{"question":"What happens when TZDIFF is called across a daylight-saving boundary?","answer":"TZDIFF ignores DST changes entirely and counts only real elapsed seconds on the UTC timeline, so a one-hour clock-change has no effect on the result."},{"question":"Which unit should I choose for cross-zone SLA or latency measurements?","answer":"Use \\\"minutes\\\" or \\\"seconds\\\" for precise SLA checks, \\\"hours\\\" for shift or flight durations, and \\\"days\\\" for multi-day turnaround reporting."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZDIFF(TZDATETIME(2024, 6, 15, 11, 30, 0, \\\"America/Los_Angeles\\\"), TZDATETIME(2024, 6, 15, 8, 0, 0, \\\"America/New_York\\\"), \\\"hours\\\")"} />

## When to use it

### When should I use TZDIFF instead of subtracting two date serials?

Use TZDIFF when the two timestamps are in different time zones or span a DST transition. Serial subtraction counts wall-clock ticks and can be off by an hour; TZDIFF works on the absolute UTC timeline so it always returns real elapsed time.

### What happens when TZDIFF is called across a daylight-saving boundary?

TZDIFF ignores DST changes entirely and counts only real elapsed seconds on the UTC timeline, so a one-hour clock-change has no effect on the result.

### Which unit should I choose for cross-zone SLA or latency measurements?

Use \"minutes\" or \"seconds\" for precise SLA checks, \"hours\" for shift or flight durations, and \"days\" for multi-day turnaround reporting.

## Use cases

**Calculate SLA breach time in minutes between two cities**

<TryIt formula={"=TZDIFF(TZDATETIME(2024, 3, 10, 14, 45, 0, \\\"Europe/Berlin\\\"), TZDATETIME(2024, 3, 10, 9, 0, 0, \\\"America/New_York\\\"), \\\"minutes\\\")"} />

**Compute elapsed days for a multi-region trade settlement**

<TryIt formula={"=TZDIFF(TZDATETIME(2024, 11, 5, 17, 0, 0, \\\"Asia/Tokyo\\\"), TZDATETIME(2024, 11, 1, 9, 30, 0, \\\"America/New_York\\\"), \\\"days\\\")"} />

## Used in

[finance](/industries/finance) · [operations](/industries/operations) · [logistics](/industries/logistics)

## Syntax

```
TZDIFF(zoned_a,zoned_b,[unit])
```

