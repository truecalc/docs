---
title: "SUBSTITUTE"
description: "Replace occurrences of a substring"
seoTitle: "SUBSTITUTE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `SUBSTITUTE` — conformant with Google Sheets and Excel. Replace occurrences of a substring"
llmsDescription: "TrueCalc SUBSTITUTE function: Replace occurrences of a substring Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use SUBSTITUTE instead of REPLACE?","answer":"Use SUBSTITUTE when you want to swap out matching text wherever it appears. Use REPLACE when you want to overwrite characters at a known position regardless of their content."},{"question":"How do I replace only the second occurrence of a character?","answer":"Pass an instance number as the optional fourth argument, for example SUBSTITUTE(text, \"-\", \"/\", 2), to change just that occurrence."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=SUBSTITUTE(\"2024-05-15\", \"-\", \"/\")"} />

## When to use it

### When should I use SUBSTITUTE instead of REPLACE?

Use SUBSTITUTE when you want to swap out matching text wherever it appears. Use REPLACE when you want to overwrite characters at a known position regardless of their content.

### How do I replace only the second occurrence of a character?

Pass an instance number as the optional fourth argument, for example SUBSTITUTE(text, "-", "/", 2), to change just that occurrence.

## Use cases

**Swap separators in a code**

<TryIt formula={"=SUBSTITUTE(\"2024-05-15\", \"-\", \"/\")"} />

**Remove thousands separators from a string**

<TryIt formula={"=SUBSTITUTE(\"1,250,000\", \",\", \"\")"} />

**Replace only the first dash**

<TryIt formula={"=SUBSTITUTE(\"a-b-c\", \"-\", \"+\", 1)"} />

## Used in

[operations](/industries/operations) · [finance](/industries/finance) · [sales](/industries/sales) · [engineering](/industries/engineering)

## Syntax

```
SUBSTITUTE(text, old, new, instance)
```

