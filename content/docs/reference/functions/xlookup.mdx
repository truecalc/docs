---
title: "XLOOKUP"
description: "Modern lookup function with fallback and match options"
seoTitle: "XLOOKUP: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `XLOOKUP` — conformant with Google Sheets and Excel. Modern lookup function with fallback and match options"
llmsDescription: "TrueCalc XLOOKUP function: Modern lookup function with fallback and match options Google Sheets/Excel conformant with fixture-verified behavior."
category: "lookup"
jsonLdFaq: [{"question":"When should I use XLOOKUP instead of VLOOKUP?","answer":"Use XLOOKUP whenever you can: it can search to the left, defaults to an exact match, and keeps working when columns are inserted or moved."},{"question":"Can XLOOKUP return a default when nothing matches?","answer":"Yes. Pass a fourth argument as the if-not-found value, e.g. XLOOKUP(key, lookup, results, Not found)."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=XLOOKUP(\"b\", {\"a\", \"b\", \"c\"}, {10, 20, 30})"} />

## When to use it

### When should I use XLOOKUP instead of VLOOKUP?

Use XLOOKUP whenever you can: it can search to the left, defaults to an exact match, and keeps working when columns are inserted or moved.

### Can XLOOKUP return a default when nothing matches?

Yes. Pass a fourth argument as the if-not-found value, e.g. XLOOKUP(key, lookup, results, Not found).

## Use cases

**Look up a value to the right of the key**

<TryIt formula={"=XLOOKUP(\"b\", {\"a\", \"b\", \"c\"}, {10, 20, 30})"} />

**Return a default when the key is missing**

<TryIt formula={"=XLOOKUP(\"z\", {\"a\", \"b\", \"c\"}, {10, 20, 30}, \"Not found\")"} />

**Find a city's region code**

<TryIt formula={"=XLOOKUP(\"Berlin\", {\"Paris\", \"Berlin\", \"Rome\"}, {\"EU-W\", \"EU-C\", \"EU-S\"})"} />

## Used in

[operations](/industries/operations) · [sales](/industries/sales) · [retail](/industries/retail) · [engineering](/industries/engineering)

## Syntax

```
XLOOKUP(search_key, lookup_array, return_array, [if_not_found], [match_mode], [search_mode])
```

## Examples

```formula
=XLOOKUP("b",{"a","b","c"},{10,20,30}) // => 20
```

