---
title: "AND"
description: "True if all arguments are true"
seoTitle: "AND: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `AND` — conformant with Google Sheets and Excel. True if all arguments are true"
llmsDescription: "TrueCalc AND function: True if all arguments are true Google Sheets/Excel conformant with fixture-verified behavior."
category: "logical"
jsonLdFaq: [{"question":"When should I use AND instead of OR?","answer":"Use AND when every condition must be true; use OR when any single condition being true is enough."},{"question":"How do I combine AND with IF?","answer":"Wrap AND in the IF test, so the IF returns its true result only when all the AND conditions hold."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=AND(TRUE, 5 > 3)"} />

## When to use it

### When should I use AND instead of OR?

Use AND when every condition must be true; use OR when any single condition being true is enough.

### How do I combine AND with IF?

Wrap AND in the IF test, so the IF returns its true result only when all the AND conditions hold.

## Use cases

**Check a value falls within a range**

<TryIt formula={"=AND(7 >= 5, 7 <= 10)"} />

**Gate an outcome on all rules passing**

<TryIt formula={"=IF(AND(8 > 5, 8 < 12), \"ok\", \"no\")"} />

## Used in

[operations](/industries/operations)

## Syntax

```
AND(value1,...)
```

