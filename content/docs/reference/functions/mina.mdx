---
title: "MINA"
description: "Minimum including booleans and text"
seoTitle: "MINA: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `MINA` — conformant with Google Sheets and Excel. Minimum including booleans and text"
llmsDescription: "TrueCalc MINA function: Minimum including booleans and text Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use MINA instead of MIN?","answer":"Use MINA when your range may contain logical values (TRUE/FALSE) or text strings and you want them counted — MINA treats TRUE as 1, FALSE and text as 0, so a text cell can become the minimum. MIN silently ignores non-numeric cells."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=MINA({1,2,3,4,5},26,4)"} />

## When to use it

### When should I use MINA instead of MIN?

Use MINA when your range may contain logical values (TRUE/FALSE) or text strings and you want them counted — MINA treats TRUE as 1, FALSE and text as 0, so a text cell can become the minimum. MIN silently ignores non-numeric cells.

## Use cases

**Minimum value across multiple arrays**

<TryIt formula={"=MINA({1,2,3,4,5},26,4)"} />

## Used in

[operations](/industries/operations) · [science](/industries/science)

## Syntax

```
MINA(value1,...)
```

## Examples

```formula
=MINA({5,10,"hello",20}) // => 0
=MINA({1,2,3,4,5},26,4) // => 1
=MINA({-5,10,"text",20}) // => -5
```

