---
title: "BIN2DEC"
description: "Convert binary to decimal"
seoTitle: "BIN2DEC: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `BIN2DEC` — conformant with Google Sheets and Excel. Convert binary to decimal"
llmsDescription: "TrueCalc BIN2DEC function: Convert binary to decimal Google Sheets/Excel conformant with fixture-verified behavior."
category: "engineering"
jsonLdFaq: [{"question":"When should I use BIN2DEC?","answer":"Use BIN2DEC to convert a binary (base-2) string to its decimal integer equivalent, for example when decoding bit-field values or permissions stored as binary strings."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=BIN2DEC(\"1100100\")"} />

## When to use it

### When should I use BIN2DEC?

Use BIN2DEC to convert a binary (base-2) string to its decimal integer equivalent, for example when decoding bit-field values or permissions stored as binary strings.

## Used in

[engineering](/industries/engineering) · [science](/industries/science)

## Syntax

```
BIN2DEC(number)
```

## Examples

```formula
=BIN2DEC("101") // => 5
=BIN2DEC("1100100") // => 100
=BIN2DEC("1111111111") // => -1
```

