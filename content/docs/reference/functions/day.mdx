---
title: "DAY"
description: "Day of month from a date serial number"
seoTitle: "DAY: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `DAY` — conformant with Google Sheets and Excel. Day of month from a date serial number"
llmsDescription: "TrueCalc DAY function: Day of month from a date serial number Google Sheets/Excel conformant with fixture-verified behavior."
category: "date"
jsonLdFaq: [{"question":"When should I use DAY instead of WEEKDAY?","answer":"Use DAY to get the day-of-month number (1-31) from a date; use WEEKDAY to get the day of the week (1-7)."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=DAY(DATE(2024, 3, 15))"} />

## When to use it

### When should I use DAY instead of WEEKDAY?

Use DAY to get the day-of-month number (1-31) from a date; use WEEKDAY to get the day of the week (1-7).

## Use cases

**Extract the day-of-month from a date**

<TryIt formula={"=DAY(DATE(2024, 3, 15))"} />

**Find the last day number of a month with EOMONTH**

<TryIt formula={"=DAY(EOMONTH(DATE(2024, 2, 1), 0))"} />

**Check whether a date falls on the first of the month**

<TryIt formula={"=IF(DAY(DATE(2024, 6, 1)) = 1, \"first\", \"not first\")"} />

## Used in

[finance](/industries/finance) · [operations](/industries/operations) · [hr](/industries/hr)

## Syntax

```
DAY(date)
```

## Examples

```formula
=DAY(DATE(1969,7,20)) // => 20
=DAY(40909) // => 1
=DAY("7/20/1969") // => 20
```

