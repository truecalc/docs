---
title: "REPLACEB"
description: "Replace portion of text by byte position"
seoTitle: "REPLACEB: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `REPLACEB` — conformant with Google Sheets and Excel. Replace portion of text by byte position"
llmsDescription: "TrueCalc REPLACEB function: Replace portion of text by byte position Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use REPLACEB instead of REPLACE?","answer":"Use REPLACEB when you need to replace a portion of a string specified by byte position and byte length, which matters when working with multibyte character sets."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

## When to use it

### When should I use REPLACEB instead of REPLACE?

Use REPLACEB when you need to replace a portion of a string specified by byte position and byte length, which matters when working with multibyte character sets.

## Used in

[operations](/industries/operations)

## Syntax

```
REPLACEB(text, start_byte, num_bytes, new_text)
```

