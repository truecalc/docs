---
title: "SUM"
description: "Sum of arguments"
seoTitle: "SUM: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `SUM` — conformant with Google Sheets and Excel. Sum of arguments"
llmsDescription: "TrueCalc SUM function: Sum of arguments Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use SUM instead of SUMIF?","answer":"Use SUM to total every value in a range; use SUMIF when you only want to total cells that meet a condition."},{"question":"Can SUM add up numbers from several ranges at once?","answer":"Yes. Pass each range or value as a separate argument, e.g. SUM(range1, range2, 10), and SUM adds them all together."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=SUM(1, 2, 3, 4)"} />

## When to use it

### When should I use SUM instead of SUMIF?

Use SUM to total every value in a range; use SUMIF when you only want to total cells that meet a condition.

### Can SUM add up numbers from several ranges at once?

Yes. Pass each range or value as a separate argument, e.g. SUM(range1, range2, 10), and SUM adds them all together.

## Use cases

**Total a short list of numbers**

<TryIt formula={"=SUM(1, 2, 3, 4)"} />

**Add up a column of values from an inline array**

<TryIt formula={"=SUM({10; 20; 30; 40})"} />

**Combine several subtotals into a grand total**

<TryIt formula={"=SUM(100, 250, 75)"} />

## Used in

[finance](/industries/finance) · [operations](/industries/operations) · [sales](/industries/sales) · [retail](/industries/retail)

## Syntax

```
SUM(value1,...)
```

## Examples

```formula
=SUM(PRICES) // => 60
=SUM(GRID) // => 10
=SUM(QUOTED_VALS) // => 300
```

