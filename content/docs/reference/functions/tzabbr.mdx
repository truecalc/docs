---
title: "TZABBR"
description: "Zone abbreviation at this instant (e.g. CEST), display only"
seoTitle: "TZABBR: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZABBR` — conformant with Google Sheets and Excel. Zone abbreviation at this instant (e.g. CEST), display only"
llmsDescription: "Use TZABBR(zoned) to get the timezone abbreviation in effect at this instant, e.g. \"EDT\", \"CEST\", \"IST\". For display only — abbreviations are not globally unique (IST is India, Ireland, and Israel). Never use for parsing; use the full IANA zone name "
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZABBR instead of the full IANA zone name for display?","answer":"Use TZABBR when you need a compact label in a UI — such as a column header or tooltip — where space is limited. Keep the full IANA name in your data; use TZABBR only at the presentation layer."},{"question":"What happens if two different zones share the same abbreviation, such as IST?","answer":"TZABBR returns the abbreviation that is correct for the specific zoned instant you pass, but the same string can mean India Standard Time, Irish Standard Time, or Israel Standard Time depending on the zone. Never use the returned abbreviation to identify or parse a zone — always keep the originating IANA name."},{"question":"Does TZABBR change its output during daylight saving time?","answer":"Yes. A zone like America/New_York returns EST in winter and EDT in summer. TZABBR reflects whichever abbreviation is in effect at the exact instant of the zoned value you supply."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZABBR(TZDATETIME(2024, 7, 15, 9, 0, 0, \"America/New_York\"))"} />

## When to use it

### When should I use TZABBR instead of the full IANA zone name for display?

Use TZABBR when you need a compact label in a UI — such as a column header or tooltip — where space is limited. Keep the full IANA name in your data; use TZABBR only at the presentation layer.

### What happens if two different zones share the same abbreviation, such as IST?

TZABBR returns the abbreviation that is correct for the specific zoned instant you pass, but the same string can mean India Standard Time, Irish Standard Time, or Israel Standard Time depending on the zone. Never use the returned abbreviation to identify or parse a zone — always keep the originating IANA name.

### Does TZABBR change its output during daylight saving time?

Yes. A zone like America/New_York returns EST in winter and EDT in summer. TZABBR reflects whichever abbreviation is in effect at the exact instant of the zoned value you supply.

## Use cases

**Show the current zone abbreviation for a Berlin timestamp**

<TryIt formula={"=TZABBR(TZDATETIME(2024, 12, 1, 14, 0, 0, \"Europe/Berlin\"))"} />

**Label a converted timestamp with its destination zone abbreviation**

<TryIt formula={"=TZABBR(TZCONVERT(TZDATETIME(2024, 3, 10, 8, 0, 0, \"UTC\"), \"Australia/Sydney\"))"} />

## Used in

[hr](/industries/hr) · [operations](/industries/operations)

## Syntax

```
TZABBR(zoned)
```

