---
title: "LAMBDA"
description: "Create a lambda function"
seoTitle: "LAMBDA: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `LAMBDA` — conformant with Google Sheets and Excel. Create a lambda function"
llmsDescription: "TrueCalc LAMBDA function: Create a lambda function Google Sheets/Excel conformant with fixture-verified behavior."
category: "logical"
jsonLdFaq: [{"question":"When should I use LAMBDA?","answer":"Use LAMBDA to define a reusable custom function inline — wrap a calculation in LAMBDA so you can call it multiple times within the same workbook without repeating the formula."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=LAMBDA(salary,salary*0.3)(1000)"} />

## When to use it

### When should I use LAMBDA?

Use LAMBDA to define a reusable custom function inline — wrap a calculation in LAMBDA so you can call it multiple times within the same workbook without repeating the formula.

## Syntax

```
LAMBDA(param1, ..., body)
```

