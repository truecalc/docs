---
title: "MAKEARRAY"
description: "Creates an array using a LAMBDA for each cell value"
seoTitle: "MAKEARRAY: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `MAKEARRAY` — conformant with Google Sheets and Excel. Creates an array using a LAMBDA for each cell value"
llmsDescription: "TrueCalc MAKEARRAY function: Creates an array using a LAMBDA for each cell value Google Sheets/Excel conformant with fixture-verified behavior."
category: "array"
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=MAKEARRAY(2,3,LAMBDA(r,c,r*c))"} />

## Syntax

```
MAKEARRAY(rows, cols, lambda)
```

## Examples

```formula
=MAKEARRAY(2,3,LAMBDA(r,c,r*c)) // => 1
=MAKEARRAY(2,2,LAMBDA(r,c,IF(r=c,1,0))) // => 1
=MAKEARRAY(3,3,LAMBDA(r,c,r+c)) // => 2
```

