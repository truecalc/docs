---
title: "JOIN"
description: "Join values with delimiter"
seoTitle: "JOIN: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `JOIN` — conformant with Google Sheets and Excel. Join values with delimiter"
llmsDescription: "TrueCalc JOIN function: Join values with delimiter Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use JOIN instead of CONCATENATE or TEXTJOIN?","answer":"Use JOIN when you want to combine multiple values or an array into a single string separated by a delimiter; it is simpler than TEXTJOIN for flat lists without blank-filtering needs."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

## When to use it

### When should I use JOIN instead of CONCATENATE or TEXTJOIN?

Use JOIN when you want to combine multiple values or an array into a single string separated by a delimiter; it is simpler than TEXTJOIN for flat lists without blank-filtering needs.

## Used in

[operations](/industries/operations) · [sales](/industries/sales)

## Syntax

```
JOIN(delimiter, value1, ...)
```

