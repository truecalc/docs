---
title: "TZTEXT"
description: "Human-friendly local string; optional strftime format"
seoTitle: "TZTEXT: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZTEXT` — conformant with Google Sheets and Excel. Human-friendly local string; optional strftime format"
llmsDescription: "Use TZTEXT(zoned) to get a human-readable local time string like \"2026-07-14 09:00:00\". An optional strftime format string (e.g. \"%A, %B %d at %H:%M\") customises the output. Use for display labels in UIs and reports. For machine-parseable output use "
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZTEXT instead of TZSTRING?","answer":"Use TZTEXT when the output is for human readers — dashboards, reports, or UI labels — where a readable format matters more than machine parseability. Use TZSTRING when the value must be parsed by another function or system."},{"question":"What happens if I omit the format argument in TZTEXT?","answer":"TZTEXT returns a readable default string such as \"2026-07-14 09:00:00\" in the local timezone of the zoned value, which is suitable for most display purposes."},{"question":"Can I use TZTEXT to show the day name and month name in a report?","answer":"Yes. Pass a strftime format like \"%A, %B %d at %H:%M\" to produce output such as \"Tuesday, July 14 at 09:00\", which is ideal for scheduled-event labels and summaries."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZTEXT(TZDATETIME(2026, 7, 14, 9, 0, 0, \"America/New_York\"))"} />

## When to use it

### When should I use TZTEXT instead of TZSTRING?

Use TZTEXT when the output is for human readers — dashboards, reports, or UI labels — where a readable format matters more than machine parseability. Use TZSTRING when the value must be parsed by another function or system.

### What happens if I omit the format argument in TZTEXT?

TZTEXT returns a readable default string such as "2026-07-14 09:00:00" in the local timezone of the zoned value, which is suitable for most display purposes.

### Can I use TZTEXT to show the day name and month name in a report?

Yes. Pass a strftime format like "%A, %B %d at %H:%M" to produce output such as "Tuesday, July 14 at 09:00", which is ideal for scheduled-event labels and summaries.

## Use cases

**Format a shift start time for a staff schedule label**

<TryIt formula={"=TZTEXT(TZDATETIME(2026, 7, 14, 6, 30, 0, \"America/Chicago\"), \"%A, %B %d at %H:%M\")"} />

**Show a sales call time in the rep's local timezone**

<TryIt formula={"=TZTEXT(TZCONVERT(TZDATETIME(2026, 7, 14, 14, 0, 0, \"UTC\"), \"America/Los_Angeles\"), \"%b %d, %Y %I:%M %p\")"} />

## Used in

[operations](/industries/operations) · [hr](/industries/hr) · [sales](/industries/sales)

## Syntax

```
TZTEXT(zoned,[format])
```

