---
title: "MONTH"
description: "Month number from a date serial number"
seoTitle: "MONTH: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `MONTH` — conformant with Google Sheets and Excel. Month number from a date serial number"
llmsDescription: "TrueCalc MONTH function: Month number from a date serial number Google Sheets/Excel conformant with fixture-verified behavior."
category: "date"
jsonLdFaq: [{"question":"What does MONTH return?","answer":"MONTH takes a date and returns its month as a number from 1 to 12, where 1 is January and 12 is December."},{"question":"How do I group transactions by month?","answer":"Apply MONTH to each date to get a 1-to-12 number, then use it as the key in a SUMIF or COUNTIF to total figures per month."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=MONTH(DATE(2024, 5, 15))"} />

## When to use it

### What does MONTH return?

MONTH takes a date and returns its month as a number from 1 to 12, where 1 is January and 12 is December.

### How do I group transactions by month?

Apply MONTH to each date to get a 1-to-12 number, then use it as the key in a SUMIF or COUNTIF to total figures per month.

## Use cases

**Find the month a sale closed**

<TryIt formula={"=MONTH(DATE(2024, 11, 28))"} />

**Extract the billing month**

<TryIt formula={"=MONTH(DATE(2024, 2, 1))"} />

## Used in

[finance](/industries/finance) · [operations](/industries/operations)

## Syntax

```
MONTH(date)
```

## Examples

```formula
=MONTH(DATE(1969,7,20)) // => 7
=MONTH(40909) // => 1
=MONTH("7/20/1969") // => 7
```

