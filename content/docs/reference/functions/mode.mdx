---
title: "MODE"
description: "Most frequent value"
seoTitle: "MODE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `MODE` — conformant with Google Sheets and Excel. Most frequent value"
llmsDescription: "TrueCalc MODE function: Most frequent value Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use MODE instead of MEDIAN or AVERAGE?","answer":"Use MODE to find the most frequently occurring value, such as the most common order size, support ticket category code, or class score. MEDIAN and AVERAGE describe the center of the data, not its most repeated value."},{"question":"What does MODE return when no value repeats?","answer":"If every value is unique, MODE returns the #N/A error because there is no most common value. Make sure your data contains at least one repeat."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=MODE(1, 2, 2, 3, 4)"} />

## When to use it

### When should I use MODE instead of MEDIAN or AVERAGE?

Use MODE to find the most frequently occurring value, such as the most common order size, support ticket category code, or class score. MEDIAN and AVERAGE describe the center of the data, not its most repeated value.

### What does MODE return when no value repeats?

If every value is unique, MODE returns the #N/A error because there is no most common value. Make sure your data contains at least one repeat.

## Use cases

**Most common value in a list**

<TryIt formula={"=MODE(1, 2, 2, 3, 4)"} />

**Most frequent rating in survey responses**

<TryIt formula={"=MODE({3, 5, 5, 5, 7, 9})"} />

**Most common order quantity**

<TryIt formula={"=MODE(10, 20, 20, 30, 30, 30)"} />

## Used in

[operations](/industries/operations) · [retail](/industries/retail) · [hr](/industries/hr) · [education](/industries/education)

## Syntax

```
MODE(value1,...)
```

## Examples

```formula
=MODE({1,2,3,3,4}) // => 3
=MODE({2,2,3,3,4}) // => 2
=MODE({1,2,3},3,4,26) // => 3
```

