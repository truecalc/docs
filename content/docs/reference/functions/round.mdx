---
title: "ROUND"
description: "Round to specified decimal places"
seoTitle: "ROUND: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `ROUND` — conformant with Google Sheets and Excel. Round to specified decimal places"
llmsDescription: "TrueCalc ROUND function: Round to specified decimal places Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use ROUND instead of ROUNDUP or ROUNDDOWN?","answer":"Use ROUND to round to the nearest value by the usual rules; use ROUNDUP or ROUNDDOWN to always force the result up or down."},{"question":"What does the second argument of ROUND control?","answer":"It sets the number of decimal places; a negative value rounds to tens, hundreds, and so on."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=ROUND(3.14159, 2)"} />

## When to use it

### When should I use ROUND instead of ROUNDUP or ROUNDDOWN?

Use ROUND to round to the nearest value by the usual rules; use ROUNDUP or ROUNDDOWN to always force the result up or down.

### What does the second argument of ROUND control?

It sets the number of decimal places; a negative value rounds to tens, hundreds, and so on.

## Use cases

**Round a price to the nearest cent**

<TryIt formula={"=ROUND(19.997, 2)"} />

**Round to the nearest hundred**

<TryIt formula={"=ROUND(1278, -2)"} />

## Used in

[science](/industries/science) · [operations](/industries/operations)

## Syntax

```
ROUND(number, digits)
```

## Examples

```formula
=ROUND(99.44,1) // => 99.4
=ROUND(99.44) // => 99
=ROUND(2.5,0) // => 3
```

