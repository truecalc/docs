---
title: "TZADD"
description: "Adds time: seconds/minutes/hours are absolute, days/weeks/months/years are DST-aware calendar units"
seoTitle: "TZADD: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZADD` — conformant with Google Sheets and Excel. Adds time: seconds/minutes/hours are absolute, days/weeks/months/years are DST-aware calendar units"
llmsDescription: "TrueCalc TZADD function: Adds time: seconds/minutes/hours are absolute, days/weeks/months/years are DST-aware calendar units Google Sheets/Excel conformant with fixture-verified behavior."
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZADD with days instead of adding 86400 seconds?","answer":"Use days when you want the same wall-clock time the next day regardless of DST. Adding 1 day across a DST boundary gives 23 or 25 real hours so the clock reads the same time, while adding 86400 seconds always moves exactly 86400 seconds forward."},{"question":"What is the difference between adding hours and adding days in TZADD?","answer":"Hours, minutes, and seconds are absolute and operate at the UTC level, so adding 24 hours always advances the instant by exactly 24 hours. Days are calendar-aware and respect DST crossings in the stored zone, so adding 1 day lands on the same wall-clock time the following day."},{"question":"What does the policy parameter control in TZADD?","answer":"The policy parameter controls how TZADD resolves the result when arithmetic lands in a DST gap or overlap — for example, choosing whether to snap forward or backward when the computed local time is ambiguous or non-existent."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZADD(TZDATETIME(2024, 3, 8, 10, 0, 0, \\\"America/New_York\\\"), 3, \\\"days\\\")"} />

## When to use it

### When should I use TZADD with days instead of adding 86400 seconds?

Use days when you want the same wall-clock time the next day regardless of DST. Adding 1 day across a DST boundary gives 23 or 25 real hours so the clock reads the same time, while adding 86400 seconds always moves exactly 86400 seconds forward.

### What is the difference between adding hours and adding days in TZADD?

Hours, minutes, and seconds are absolute and operate at the UTC level, so adding 24 hours always advances the instant by exactly 24 hours. Days are calendar-aware and respect DST crossings in the stored zone, so adding 1 day lands on the same wall-clock time the following day.

### What does the policy parameter control in TZADD?

The policy parameter controls how TZADD resolves the result when arithmetic lands in a DST gap or overlap — for example, choosing whether to snap forward or backward when the computed local time is ambiguous or non-existent.

## Use cases

**Calculate a meeting reminder 90 minutes before a zoned event**

<TryIt formula={"=TZADD(TZDATETIME(2024, 6, 15, 14, 30, 0, \\\"Europe/London\\\"), -90, \\\"minutes\\\")"} />

**Advance a financial settlement date by one month**

<TryIt formula={"=TZADD(TZDATETIME(2024, 1, 31, 9, 0, 0, \\\"America/Chicago\\\"), 1, \\\"months\\\")"} />

## Used in

[operations](/industries/operations) · [finance](/industries/finance) · [logistics](/industries/logistics)

## Syntax

```
TZADD(zoned,amount,unit,[policy])
```

