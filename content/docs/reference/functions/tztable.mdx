---
title: "TZTABLE"
description: "Alias of TZBOARD"
seoTitle: "TZTABLE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZTABLE` — conformant with Google Sheets and Excel. Alias of TZBOARD"
llmsDescription: "TrueCalc TZTABLE function: Alias of TZBOARD Google Sheets/Excel conformant with fixture-verified behavior."
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZTABLE instead of TZBOARD?","answer":"Prefer TZBOARD in all new formulas. Use TZTABLE only when maintaining legacy sheets that already reference it — both functions return identical results, so there is no behavioral reason to choose TZTABLE over TZBOARD."},{"question":"What happens if I mix TZTABLE and TZBOARD calls in the same workbook?","answer":"They return the same structured world-clock data, so mixing them is safe. For consistency, prefer one name throughout a workbook — TZBOARD is the canonical name."},{"question":"Does TZTABLE accept the same arguments as TZBOARD?","answer":"Yes. TZTABLE is a direct alias of TZBOARD and accepts the same anchor datetime and one or more zone identifiers, producing identical output."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZTABLE(TZDATETIME(2024, 9, 10, 9, 0, 0, \\\"UTC\\\"), \\\"America/New_York\\\", \\\"Europe/Amsterdam\\\", \\\"Asia/Singapore\\\")"} />

## When to use it

### When should I use TZTABLE instead of TZBOARD?

Prefer TZBOARD in all new formulas. Use TZTABLE only when maintaining legacy sheets that already reference it — both functions return identical results, so there is no behavioral reason to choose TZTABLE over TZBOARD.

### What happens if I mix TZTABLE and TZBOARD calls in the same workbook?

They return the same structured world-clock data, so mixing them is safe. For consistency, prefer one name throughout a workbook — TZBOARD is the canonical name.

### Does TZTABLE accept the same arguments as TZBOARD?

Yes. TZTABLE is a direct alias of TZBOARD and accepts the same anchor datetime and one or more zone identifiers, producing identical output.

## Use cases

**Compare shift handoff times across global operations centers**

<TryIt formula={"=TZTABLE(TZDATETIME(2024, 6, 1, 17, 0, 0, \\\"Europe/London\\\"), \\\"America/Chicago\\\", \\\"Asia/Tokyo\\\")"} />

**Snapshot current UTC time across regional offices**

<TryIt formula={"=TZTABLE(TZNOW(), \\\"America/Los_Angeles\\\", \\\"Asia/Dubai\\\", \\\"Pacific/Auckland\\\")"} />

## Used in

[engineering](/industries/engineering) · [operations](/industries/operations)

## Syntax

```
TZTABLE(anchor,zones,[base])
```

