---
title: "VDB"
description: "Variable-rate declining balance"
seoTitle: "VDB: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `VDB` — conformant with Google Sheets and Excel. Variable-rate declining balance"
llmsDescription: "TrueCalc VDB function: Variable-rate declining balance Google Sheets/Excel conformant with fixture-verified behavior."
category: "financial"
jsonLdFaq: [{"question":"When should I use VDB?","answer":"Use VDB to calculate depreciation over any partial or full period using a variable declining-balance method — more flexible than DB or DDB because it can span fractional periods and optionally switch to straight-line when that yields a higher deduction."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=VDB(100,10,20,10,11,2,TRUE)"} />

## When to use it

### When should I use VDB?

Use VDB to calculate depreciation over any partial or full period using a variable declining-balance method — more flexible than DB or DDB because it can span fractional periods and optionally switch to straight-line when that yields a higher deduction.

## Used in

[finance](/industries/finance)

## Syntax

```
VDB(cost, salvage, life, start, end, [factor], [no_switch])
```

## Examples

```formula
=VDB(100,10,20,10,11,2,TRUE) // => 3.4867844010000013
=VDB(100,33,20,10,11,2,FALSE) // => 1.867844010000013
```

