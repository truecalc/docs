---
title: "MODE.MULT"
description: "All most frequent values"
seoTitle: "MODE.MULT: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `MODE.MULT` — conformant with Google Sheets and Excel. All most frequent values"
llmsDescription: "TrueCalc MODE.MULT function: All most frequent values Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use MODE.MULT instead of MODE.SNGL?","answer":"Use MODE.MULT when your data may have multiple values that all tie for the highest frequency and you want all of them returned as an array — for example, when two products sell equally often. MODE.SNGL returns only the first such value."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=MODE.MULT({10,15,20,30,10,15})"} />

## When to use it

### When should I use MODE.MULT instead of MODE.SNGL?

Use MODE.MULT when your data may have multiple values that all tie for the highest frequency and you want all of them returned as an array — for example, when two products sell equally often. MODE.SNGL returns only the first such value.

## Used in

[operations](/industries/operations) · [science](/industries/science)

## Syntax

```
MODE.MULT(value1,...)
```

## Examples

```formula
=MODE.MULT({10,15,20,30,10,15}) // => 10
```

