---
title: "TEXTJOIN"
description: "Join values with delimiter, optionally skip empty"
seoTitle: "TEXTJOIN: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TEXTJOIN` — conformant with Google Sheets and Excel. Join values with delimiter, optionally skip empty"
llmsDescription: "TrueCalc TEXTJOIN function: Join values with delimiter, optionally skip empty Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use TEXTJOIN instead of the & operator?","answer":"Use TEXTJOIN when you are joining many values with the same separator and want to skip blanks automatically; use & for quick one-off concatenations."},{"question":"How do I make TEXTJOIN skip empty values?","answer":"Set the second argument (ignore_empty) to TRUE and blank cells are left out, so you do not get doubled-up separators."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TEXTJOIN(\", \", TRUE, \"a\", \"b\", \"c\")"} />

## When to use it

### When should I use TEXTJOIN instead of the & operator?

Use TEXTJOIN when you are joining many values with the same separator and want to skip blanks automatically; use & for quick one-off concatenations.

### How do I make TEXTJOIN skip empty values?

Set the second argument (ignore_empty) to TRUE and blank cells are left out, so you do not get doubled-up separators.

## Use cases

**Build a full name from parts**

<TryIt formula={"=TEXTJOIN(\" \", TRUE, \"Ada\", \"Lovelace\")"} />

**Combine tags while skipping blanks**

<TryIt formula={"=TEXTJOIN(\" | \", TRUE, \"red\", \"\", \"blue\", \"green\")"} />

## Used in

[operations](/industries/operations) · [sales](/industries/sales) · [retail](/industries/retail)

## Syntax

```
TEXTJOIN(delimiter, ignore_empty, value1, ...)
```

