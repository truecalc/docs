---
title: "NORM.DIST"
description: "Normal CDF/PDF"
seoTitle: "NORM.DIST: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `NORM.DIST` — conformant with Google Sheets and Excel. Normal CDF/PDF"
llmsDescription: "TrueCalc NORM.DIST function: Normal CDF/PDF Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use NORM.DIST?","answer":"Use NORM.DIST to calculate the probability that a normally distributed variable falls at or below a given value (cumulative=TRUE), or the probability density at that value (cumulative=FALSE) — useful for grading on a curve, process capability, or confidence interval calculations."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=NORM.DIST(2.4,1,4,TRUE)"} />

## When to use it

### When should I use NORM.DIST?

Use NORM.DIST to calculate the probability that a normally distributed variable falls at or below a given value (cumulative=TRUE), or the probability density at that value (cumulative=FALSE) — useful for grading on a curve, process capability, or confidence interval calculations.

## Use cases

**Cumulative probability at x=2.4 with mean=1 and stdev=4**

<TryIt formula={"=NORM.DIST(2.4,1,4,TRUE)"} />

## Used in

[science](/industries/science) · [operations](/industries/operations)

## Syntax

```
NORM.DIST(x,mean,stdev,cumulative)
```

## Examples

```formula
=NORM.DIST(2.4,1,4,FALSE) // => 0.09381008672923448
=NORM.DIST(2.4,1,4,TRUE) // => 0.6368306511756191
=NORM.DIST(0,0,1,TRUE) // => 0.5
```

