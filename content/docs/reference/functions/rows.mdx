---
title: "ROWS"
description: "Returns the number of rows in an array or range"
seoTitle: "ROWS: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `ROWS` — conformant with Google Sheets and Excel. Returns the number of rows in an array or range"
llmsDescription: "TrueCalc ROWS function: Returns the number of rows in an array or range Google Sheets/Excel conformant with fixture-verified behavior."
category: "array"
jsonLdFaq: [{"question":"When should I use ROWS?","answer":"Use ROWS to count the number of rows in an array or range, often used in dynamic formulas to adapt to variable-height data."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=ROWS({1;2;3})"} />

## When to use it

### When should I use ROWS?

Use ROWS to count the number of rows in an array or range, often used in dynamic formulas to adapt to variable-height data.

## Use cases

**Count rows in a literal vertical array**

<TryIt formula={"=ROWS({1;2;3})"} />

## Used in

[operations](/industries/operations) · [science](/industries/science)

## Syntax

```
ROWS(array)
```

## Examples

```formula
=ROWS(PRICES) // => 3
=ROWS(INDIRECT("A1")) // => 1
=ROWS(INDIRECT("A1:A5")) // => 5
```

