---
title: "ISOWEEKNUM"
description: "ISO week number of the year for a date"
seoTitle: "ISOWEEKNUM: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `ISOWEEKNUM` — conformant with Google Sheets and Excel. ISO week number of the year for a date"
llmsDescription: "TrueCalc ISOWEEKNUM function: ISO week number of the year for a date Google Sheets/Excel conformant with fixture-verified behavior."
category: "date"
jsonLdFaq: [{"question":"When should I use ISOWEEKNUM instead of WEEKNUM?","answer":"Use ISOWEEKNUM when you need ISO 8601 week numbers (week starts Monday, week 1 is the week containing the year's first Thursday), which is the standard used in Europe and international supply chains."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=ISOWEEKNUM(DATE(2002,6,14))"} />

## When to use it

### When should I use ISOWEEKNUM instead of WEEKNUM?

Use ISOWEEKNUM when you need ISO 8601 week numbers (week starts Monday, week 1 is the week containing the year's first Thursday), which is the standard used in Europe and international supply chains.

## Use cases

**ISO week number for a specific date**

<TryIt formula={"=ISOWEEKNUM(DATE(2002,6,14))"} />

## Used in

[operations](/industries/operations) · [engineering](/industries/engineering)

## Syntax

```
ISOWEEKNUM(date)
```

## Examples

```formula
=ISOWEEKNUM(DATE(1969,7,20)) // => 29
=ISOWEEKNUM(DATE(2002,6,14)) // => 24
=ISOWEEKNUM(DATE(2015,1,1)) // => 1
```

