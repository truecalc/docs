---
title: "TZOFFSETDIFF"
description: "Signed difference in minutes between two zones' offsets"
seoTitle: "TZOFFSETDIFF: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZOFFSETDIFF` — conformant with Google Sheets and Excel. Signed difference in minutes between two zones' offsets"
llmsDescription: "Use TZOFFSETDIFF(zoned_a,zoned_b) to get the signed difference in UTC offset minutes between two zone-aware instants. Positive if a is ahead of b. Berlin vs New York in summer returns +360 (Berlin is 6 h ahead). Useful for \"X hours ahead/behind\" disp"
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZOFFSETDIFF instead of TZDIFF to compare two zone-aware times?","answer":"Use TZOFFSETDIFF when you want to know how many minutes one zone is ahead or behind another at a specific instant — for example, to display 'Berlin is 6 hours ahead of New York'. TZDIFF measures real elapsed time between two events; TZOFFSETDIFF measures only the offset gap between two zones."},{"question":"What happens if both instants share the same UTC offset at that moment?","answer":"TZOFFSETDIFF returns 0. This can occur when two different named zones happen to have the same DST rule in effect at that instant, or when both zones are identical."},{"question":"Can TZOFFSETDIFF return a negative number?","answer":"Yes. The result is signed: positive when zone A is ahead of zone B (further east or using DST while B does not), and negative when zone A is behind zone B."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZOFFSETDIFF(TZDATETIME(2024, 7, 15, 12, 0, 0, \"Europe/Berlin\"), TZDATETIME(2024, 7, 15, 12, 0, 0, \"America/New_York\"))"} />

## When to use it

### When should I use TZOFFSETDIFF instead of TZDIFF to compare two zone-aware times?

Use TZOFFSETDIFF when you want to know how many minutes one zone is ahead or behind another at a specific instant — for example, to display 'Berlin is 6 hours ahead of New York'. TZDIFF measures real elapsed time between two events; TZOFFSETDIFF measures only the offset gap between two zones.

### What happens if both instants share the same UTC offset at that moment?

TZOFFSETDIFF returns 0. This can occur when two different named zones happen to have the same DST rule in effect at that instant, or when both zones are identical.

### Can TZOFFSETDIFF return a negative number?

Yes. The result is signed: positive when zone A is ahead of zone B (further east or using DST while B does not), and negative when zone A is behind zone B.

## Use cases

**Check offset gap between Tokyo and London during European winter**

<TryIt formula={"=TZOFFSETDIFF(TZDATETIME(2024, 1, 10, 9, 0, 0, \"Asia/Tokyo\"), TZDATETIME(2024, 1, 10, 9, 0, 0, \"Europe/London\"))"} />

**Detect whether a US East Coast office is ahead or behind US West Coast**

<TryIt formula={"=TZOFFSETDIFF(TZDATETIME(2024, 3, 12, 10, 0, 0, \"America/New_York\"), TZDATETIME(2024, 3, 12, 10, 0, 0, \"America/Los_Angeles\"))"} />

## Used in

[hr](/industries/hr) · [operations](/industries/operations) · [engineering](/industries/engineering)

## Syntax

```
TZOFFSETDIFF(zoned_a,zoned_b)
```

