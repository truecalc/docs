---
title: "ERROR.TYPE"
description: "Number corresponding to the error type"
seoTitle: "ERROR.TYPE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `ERROR.TYPE` — conformant with Google Sheets and Excel. Number corresponding to the error type"
llmsDescription: "TrueCalc ERROR.TYPE function: Number corresponding to the error type Google Sheets/Excel conformant with fixture-verified behavior."
category: "logical"
jsonLdFaq: [{"question":"When should I use ERROR.TYPE?","answer":"Use ERROR.TYPE to identify which specific error a formula produced — it returns a number (1=#NULL!, 2=#DIV/0!, 3=#VALUE!, etc.) so you can branch on the error kind in IF or SWITCH logic."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=ERROR.TYPE(1/0)"} />

## When to use it

### When should I use ERROR.TYPE?

Use ERROR.TYPE to identify which specific error a formula produced — it returns a number (1=#NULL!, 2=#DIV/0!, 3=#VALUE!, etc.) so you can branch on the error kind in IF or SWITCH logic.

## Used in

[operations](/industries/operations)

## Syntax

```
ERROR.TYPE(value)
```

## Examples

```formula
=ERROR.TYPE(1/0) // => 2
=ERROR.TYPE(VALUE("x")) // => 3
=ERROR.TYPE(NA()) // => 7
```

