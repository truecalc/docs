---
title: "SEQUENCE"
description: "Generate a sequence of numbers"
seoTitle: "SEQUENCE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `SEQUENCE` — conformant with Google Sheets and Excel. Generate a sequence of numbers"
llmsDescription: "TrueCalc SEQUENCE function: Generate a sequence of numbers Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use SEQUENCE instead of manually listing numbers?","answer":"Use SEQUENCE when you need to generate a series of numbers automatically, such as row numbers, step indices, or evenly spaced values, without typing each one."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=SEQUENCE(5,1,0,2)"} />

## When to use it

### When should I use SEQUENCE instead of manually listing numbers?

Use SEQUENCE when you need to generate a series of numbers automatically, such as row numbers, step indices, or evenly spaced values, without typing each one.

## Use cases

**Generate a 5-step sequence starting at 0 with step 2**

<TryIt formula={"=SEQUENCE(5,1,0,2)"} />

## Used in

[science](/industries/science) · [engineering](/industries/engineering) · [education](/industries/education)

## Syntax

```
SEQUENCE(rows, [cols], [start], [step])
```

## Examples

```formula
=SEQUENCE(2) // => 1
=SEQUENCE(2,3) // => 1
=SEQUENCE(2,3,3,2) // => 3
```

