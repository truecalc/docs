---
title: "LENB"
description: "Number of bytes in text"
seoTitle: "LENB: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `LENB` — conformant with Google Sheets and Excel. Number of bytes in text"
llmsDescription: "TrueCalc LENB function: Number of bytes in text Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use LENB instead of LEN?","answer":"Use LENB when you need the byte length of a string rather than the character count, which differs for multibyte character sets such as CJK where each character is 2 bytes."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=LENB(\"Aen\")"} />

## When to use it

### When should I use LENB instead of LEN?

Use LENB when you need the byte length of a string rather than the character count, which differs for multibyte character sets such as CJK where each character is 2 bytes.

## Used in

[operations](/industries/operations)

## Syntax

```
LENB(text)
```

## Examples

```formula
=LENB("Aeñ") // => 3
=LENB("熊本") // => 4
```

