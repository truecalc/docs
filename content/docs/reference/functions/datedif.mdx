---
title: "DATEDIF"
description: "Difference between two dates in specified units"
seoTitle: "DATEDIF: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `DATEDIF` — conformant with Google Sheets and Excel. Difference between two dates in specified units"
llmsDescription: "TrueCalc DATEDIF function: Difference between two dates in specified units Google Sheets/Excel conformant with fixture-verified behavior."
category: "date"
jsonLdFaq: [{"question":"When should I use DATEDIF?","answer":"Use DATEDIF to measure the gap between two dates in whole years, months, or days — handy for ages, tenure, and durations."},{"question":"What does the unit argument in DATEDIF mean?","answer":"It sets the unit of the result: Y for full years, M for full months, and D for total days between the two dates."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=DATEDIF(DATE(1990, 1, 1), DATE(2020, 1, 1), \"Y\")"} />

## When to use it

### When should I use DATEDIF?

Use DATEDIF to measure the gap between two dates in whole years, months, or days — handy for ages, tenure, and durations.

### What does the unit argument in DATEDIF mean?

It sets the unit of the result: Y for full years, M for full months, and D for total days between the two dates.

## Use cases

**Months of employee tenure**

<TryIt formula={"=DATEDIF(DATE(2022, 1, 1), DATE(2023, 7, 1), \"M\")"} />

**Total days in a project window**

<TryIt formula={"=DATEDIF(DATE(2024, 1, 1), DATE(2024, 3, 1), \"D\")"} />

## Used in

[hr](/industries/hr) · [operations](/industries/operations)

## Syntax

```
DATEDIF(start,end,unit)
```

## Examples

```formula
=DATEDIF(DATE(1969,7,16),DATE(1969,7,24),"D") // => 8
=DATEDIF(DATE(2000,1,1),DATE(2001,1,1),"M") // => 12
=DATEDIF(DATE(2001,1,1),DATE(2003,1,1),"Y") // => 2
```

