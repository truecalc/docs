---
title: "COUNTIFS"
description: "Count rows where all criteria match"
seoTitle: "COUNTIFS: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `COUNTIFS` — conformant with Google Sheets and Excel. Count rows where all criteria match"
llmsDescription: "TrueCalc COUNTIFS function: Count rows where all criteria match Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use COUNTIFS instead of COUNTIF?","answer":"Use COUNTIFS to count rows that satisfy several conditions at once; use COUNTIF when you have just one condition."},{"question":"Do all the conditions in COUNTIFS have to match?","answer":"Yes. COUNTIFS only counts an item when every range/criteria pair it lists is satisfied for that position."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=COUNTIFS({1, 2, 3, 4}, \">2\")"} />

## When to use it

### When should I use COUNTIFS instead of COUNTIF?

Use COUNTIFS to count rows that satisfy several conditions at once; use COUNTIF when you have just one condition.

### Do all the conditions in COUNTIFS have to match?

Yes. COUNTIFS only counts an item when every range/criteria pair it lists is satisfied for that position.

## Use cases

**Count rows matching two conditions**

<TryIt formula={"=COUNTIFS({1, 2, 3, 4}, \">1\", {5, 6, 7, 8}, \"<8\")"} />

**Count items in a target range**

<TryIt formula={"=COUNTIFS({10, 20, 30, 40}, \">=20\", {10, 20, 30, 40}, \"<=30\")"} />

## Used in

[operations](/industries/operations)

## Syntax

```
COUNTIFS(range1, criterion1, ...)
```

## Examples

```formula
=COUNTIFS({10,25,30,15,22},">20",{10,25,30,15,22},"<30") // => 2
=COUNTIFS({"Yes","No","Yes","Yes"},"Yes") // => 3
=COUNTIFS({1,2,3,4,5},">2",{10,20,30,40,50},"<45") // => 2
```

