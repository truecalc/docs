---
title: "VSTACK"
description: "Vertically stacks arrays"
seoTitle: "VSTACK: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `VSTACK` — conformant with Google Sheets and Excel. Vertically stacks arrays"
llmsDescription: "TrueCalc VSTACK function: Vertically stacks arrays Google Sheets/Excel conformant with fixture-verified behavior."
category: "array"
jsonLdFaq: [{"question":"When should I use VSTACK?","answer":"Use VSTACK to combine two or more arrays vertically into a single array — useful for merging data from separate table sections or combining monthly datasets into one continuous block."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=VSTACK({1,2},{3,4})"} />

## When to use it

### When should I use VSTACK?

Use VSTACK to combine two or more arrays vertically into a single array — useful for merging data from separate table sections or combining monthly datasets into one continuous block.

## Use cases

**Combine two row arrays into one column**

<TryIt formula={"=VSTACK({1,2},{3,4})"} />

## Used in

[operations](/industries/operations)

## Syntax

```
VSTACK(array1, ...)
```

## Examples

```formula
=VSTACK({1,2},{3,4}) // => 1
=VSTACK({1,2;3,4},{5,6;7,8}) // => 1
=VSTACK({1,2},{3,4},{5,6}) // => 1
```

