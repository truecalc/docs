---
title: "LOOKUP"
description: "Approximate lookup in sorted range"
seoTitle: "LOOKUP: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `LOOKUP` — conformant with Google Sheets and Excel. Approximate lookup in sorted range"
llmsDescription: "TrueCalc LOOKUP function: Approximate lookup in sorted range Google Sheets/Excel conformant with fixture-verified behavior."
category: "lookup"
jsonLdFaq: [{"question":"When should I use LOOKUP instead of VLOOKUP?","answer":"Use LOOKUP for a simple search across a single sorted vector returning a parallel result vector; use VLOOKUP to pull a column from a table by a key."},{"question":"Does LOOKUP need its search data sorted?","answer":"Yes. LOOKUP assumes the search vector is sorted in ascending order and returns the largest value at or below the lookup value."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=LOOKUP(2, {1, 2, 3}, {\"a\", \"b\", \"c\"})"} />

## When to use it

### When should I use LOOKUP instead of VLOOKUP?

Use LOOKUP for a simple search across a single sorted vector returning a parallel result vector; use VLOOKUP to pull a column from a table by a key.

### Does LOOKUP need its search data sorted?

Yes. LOOKUP assumes the search vector is sorted in ascending order and returns the largest value at or below the lookup value.

## Use cases

**Look up a value in a parallel result vector**

<TryIt formula={"=LOOKUP(2, {1, 2, 3}, {\"a\", \"b\", \"c\"})"} />

**Map a score to a letter grade band**

<TryIt formula={"=LOOKUP(85, {0, 60, 70, 80, 90}, {\"F\", \"D\", \"C\", \"B\", \"A\"})"} />

**Find a tier price from sorted quantity breaks**

<TryIt formula={"=LOOKUP(150, {1, 100, 500}, {9.99, 8.49, 6.99})"} />

## Used in

[operations](/industries/operations) · [sales](/industries/sales) · [education](/industries/education) · [retail](/industries/retail)

## Syntax

```
LOOKUP(search_key, search_range, [result_range])
```

