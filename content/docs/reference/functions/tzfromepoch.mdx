---
title: "TZFROMEPOCH"
description: "Builds a zone-aware instant from a Unix timestamp"
seoTitle: "TZFROMEPOCH: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZFROMEPOCH` — conformant with Google Sheets and Excel. Builds a zone-aware instant from a Unix timestamp"
llmsDescription: "Use TZFROMEPOCH(unix_seconds,zone) to convert a Unix timestamp (seconds since 1970-01-01 UTC) into a zone-aware instant displayed in the given IANA zone. Useful when ingesting API or database timestamps. Compose with TZCONVERT or TZWORLDCLOCK to disp"
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZFROMEPOCH instead of TZDATETIME?","answer":"Use TZFROMEPOCH when your source data is a Unix timestamp (integer seconds since 1970-01-01T00:00:00Z) from an API or database. Use TZDATETIME when you already have a human-readable date and time."},{"question":"What does the zone argument do in TZFROMEPOCH?","answer":"It stamps the resulting instant with an IANA timezone for display purposes, converting the UTC epoch into local wall-clock time in that zone."},{"question":"What happens if I pass a negative Unix timestamp to TZFROMEPOCH?","answer":"Negative values represent instants before 1970-01-01T00:00:00Z, so TZFROMEPOCH correctly builds dates in 1969 or earlier."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZFROMEPOCH(1700000000, \"America/New_York\")"} />

## When to use it

### When should I use TZFROMEPOCH instead of TZDATETIME?

Use TZFROMEPOCH when your source data is a Unix timestamp (integer seconds since 1970-01-01T00:00:00Z) from an API or database. Use TZDATETIME when you already have a human-readable date and time.

### What does the zone argument do in TZFROMEPOCH?

It stamps the resulting instant with an IANA timezone for display purposes, converting the UTC epoch into local wall-clock time in that zone.

### What happens if I pass a negative Unix timestamp to TZFROMEPOCH?

Negative values represent instants before 1970-01-01T00:00:00Z, so TZFROMEPOCH correctly builds dates in 1969 or earlier.

## Use cases

**Convert a database epoch to Tokyo time**

<TryIt formula={"=TZFROMEPOCH(1688169600, \"Asia/Tokyo\")"} />

**Display a Unix timestamp in UTC**

<TryIt formula={"=TZFROMEPOCH(1672531200, \"UTC\")"} />

## Used in

[engineering](/industries/engineering) · [operations](/industries/operations) · [logistics](/industries/logistics)

## Syntax

```
TZFROMEPOCH(unix_seconds,zone)
```

