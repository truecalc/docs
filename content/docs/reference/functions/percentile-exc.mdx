---
title: "PERCENTILE.EXC"
description: "Exclusive percentile"
seoTitle: "PERCENTILE.EXC: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `PERCENTILE.EXC` — conformant with Google Sheets and Excel. Exclusive percentile"
llmsDescription: "TrueCalc PERCENTILE.EXC function: Exclusive percentile Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use PERCENTILE.EXC instead of PERCENTILE.INC?","answer":"Use PERCENTILE.EXC when you need the exclusive percentile, where k must be strictly between 0 and 1 (endpoints excluded). This matches some statistical conventions — for example, rank-based methods that treat boundary percentiles as undefined."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=PERCENTILE.EXC({15,20,35,40,50},0.4)"} />

## When to use it

### When should I use PERCENTILE.EXC instead of PERCENTILE.INC?

Use PERCENTILE.EXC when you need the exclusive percentile, where k must be strictly between 0 and 1 (endpoints excluded). This matches some statistical conventions — for example, rank-based methods that treat boundary percentiles as undefined.

## Use cases

**40th exclusive percentile of a five-value set**

<TryIt formula={"=PERCENTILE.EXC({15,20,35,40,50},0.4)"} />

## Used in

[science](/industries/science) · [operations](/industries/operations)

## Syntax

```
PERCENTILE.EXC(array,k)
```

## Examples

```formula
=PERCENTILE.EXC({15,20,35,40,50},0.4) // => 26.000000000000007
=PERCENTILE.EXC({15,20,35,40,50},0.5) // => 35
=PERCENTILE.EXC({15,20,35,40,50},0.8) // => 48.00000000000001
```

