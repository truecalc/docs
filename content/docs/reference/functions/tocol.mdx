---
title: "TOCOL"
description: "Converts an array to a single column"
seoTitle: "TOCOL: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TOCOL` — conformant with Google Sheets and Excel. Converts an array to a single column"
llmsDescription: "TrueCalc TOCOL function: Converts an array to a single column Google Sheets/Excel conformant with fixture-verified behavior."
category: "array"
jsonLdFaq: [{"question":"When should I use TOCOL?","answer":"Use TOCOL to reshape a 2-D array or multi-row range into a single column — useful when you need to feed a flat list of values into UNIQUE, SORT, or other single-column functions."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TOCOL({1,2;3,4})"} />

## When to use it

### When should I use TOCOL?

Use TOCOL to reshape a 2-D array or multi-row range into a single column — useful when you need to feed a flat list of values into UNIQUE, SORT, or other single-column functions.

## Used in

[operations](/industries/operations)

## Syntax

```
TOCOL(array, [ignore], [scan_by_col])
```

## Examples

```formula
=TOCOL({1,2;3,4}) // => 1
=TOCOL({1,2,3}) // => 1
=TOCOL({1,2,3;4,5,6;7,8,9}) // => 1
```

