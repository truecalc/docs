---
title: "SPLIT"
description: "Split text into array by delimiter"
seoTitle: "SPLIT: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `SPLIT` — conformant with Google Sheets and Excel. Split text into array by delimiter"
llmsDescription: "TrueCalc SPLIT function: Split text into array by delimiter Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use SPLIT?","answer":"Use SPLIT to break a delimited text string into an array of separate values, such as parsing a CSV row or splitting a full name into parts."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=SPLIT(\"1,2,3\", \",\")"} />

## When to use it

### When should I use SPLIT?

Use SPLIT to break a delimited text string into an array of separate values, such as parsing a CSV row or splitting a full name into parts.

## Use cases

**Split a comma-delimited list into an array**

<TryIt formula={"=SPLIT(\"1,2,3\", \",\")"} />

## Used in

[operations](/industries/operations) · [sales](/industries/sales)

## Syntax

```
SPLIT(text, delimiter, [split_by_each], [remove_empty])
```

## Examples

```formula
=SPLIT("1,2,3",",") // => 1
```

