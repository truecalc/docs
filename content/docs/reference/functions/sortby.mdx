---
title: "SORTBY"
description: "Sorts an array based on the values in corresponding arrays"
seoTitle: "SORTBY: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `SORTBY` — conformant with Google Sheets and Excel. Sorts an array based on the values in corresponding arrays"
llmsDescription: "TrueCalc SORTBY function: Sorts an array based on the values in corresponding arrays Google Sheets/Excel conformant with fixture-verified behavior."
category: "array"
jsonLdFaq: [{"question":"When should I use SORTBY?","answer":"Use SORTBY to sort an array based on the values in one or more separate corresponding arrays, which lets you order data by an external key without it being part of the returned array."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

## When to use it

### When should I use SORTBY?

Use SORTBY to sort an array based on the values in one or more separate corresponding arrays, which lets you order data by an external key without it being part of the returned array.

## Used in

[operations](/industries/operations) · [science](/industries/science)

## Syntax

```
SORTBY(array, by_array1, [sort_order1], ...)
```

