---
title: "ISNUMBER"
description: "True if value is a number"
seoTitle: "ISNUMBER: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `ISNUMBER` — conformant with Google Sheets and Excel. True if value is a number"
llmsDescription: "TrueCalc ISNUMBER function: True if value is a number Google Sheets/Excel conformant with fixture-verified behavior."
category: "logical"
jsonLdFaq: [{"question":"When should I use ISNUMBER?","answer":"Use ISNUMBER to test whether a cell holds a real number before doing math on it, often inside an IF to guard against text values."},{"question":"Does ISNUMBER treat a number stored as text as a number?","answer":"No — a numeric string like the text 100 is not a number, so ISNUMBER returns FALSE until you convert it with VALUE."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=ISNUMBER(5)"} />

## When to use it

### When should I use ISNUMBER?

Use ISNUMBER to test whether a cell holds a real number before doing math on it, often inside an IF to guard against text values.

### Does ISNUMBER treat a number stored as text as a number?

No — a numeric string like the text 100 is not a number, so ISNUMBER returns FALSE until you convert it with VALUE.

## Use cases

**Confirm text is not mistaken for a number**

<TryIt formula={"=ISNUMBER(\"text\")"} />

**Verify a converted string is now numeric**

<TryIt formula={"=ISNUMBER(VALUE(\"100\"))"} />

## Used in

[operations](/industries/operations)

## Syntax

```
ISNUMBER(value)
```

