---
title: "TEXT"
description: "Format number as text"
seoTitle: "TEXT: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TEXT` — conformant with Google Sheets and Excel. Format number as text"
llmsDescription: "TrueCalc TEXT function: Format number as text Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"What does TEXT do?","answer":"TEXT converts a number or date into text formatted by a pattern you supply, such as a currency, percentage, or date layout. The result is text, not a number."},{"question":"When should I avoid TEXT?","answer":"Avoid TEXT when you still need to do math on the value. Formatting turns the number into text, so apply TEXT only at the point where you want a display string."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TEXT(1234.5, \"$#,##0.00\")"} />

## When to use it

### What does TEXT do?

TEXT converts a number or date into text formatted by a pattern you supply, such as a currency, percentage, or date layout. The result is text, not a number.

### When should I avoid TEXT?

Avoid TEXT when you still need to do math on the value. Formatting turns the number into text, so apply TEXT only at the point where you want a display string.

## Use cases

**Show a ratio as a percentage**

<TryIt formula={"=TEXT(0.0825, \"0.0%\")"} />

**Format a large number with thousands separators**

<TryIt formula={"=TEXT(2500000, \"#,##0\")"} />

## Used in

[retail](/industries/retail) · [operations](/industries/operations)

## Syntax

```
TEXT(value, format)
```

