---
title: "SQRT"
description: "Square root"
seoTitle: "SQRT: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `SQRT` — conformant with Google Sheets and Excel. Square root"
llmsDescription: "TrueCalc SQRT function: Square root Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use SQRT instead of POWER?","answer":"Use SQRT for the square root of a single value; it reads more clearly than POWER(x, 0.5). Use POWER when you need other roots or exponents."},{"question":"What happens if I pass a negative number to SQRT?","answer":"SQRT returns the #NUM! error for negative inputs because there is no real square root. Wrap the argument with ABS first if you only care about magnitude."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=SQRT(144)"} />

## When to use it

### When should I use SQRT instead of POWER?

Use SQRT for the square root of a single value; it reads more clearly than POWER(x, 0.5). Use POWER when you need other roots or exponents.

### What happens if I pass a negative number to SQRT?

SQRT returns the #NUM! error for negative inputs because there is no real square root. Wrap the argument with ABS first if you only care about magnitude.

## Use cases

**Irrational root used in geometry**

<TryIt formula={"=SQRT(2)"} />

**Hypotenuse length from two legs**

<TryIt formula={"=SQRT(POWER(3,2) + POWER(4,2))"} />

## Used in

[science](/industries/science) · [engineering](/industries/engineering)

## Syntax

```
SQRT(number)
```

## Examples

```formula
=SQRT(9) // => 3
```

