---
title: "TZLOCALIZE"
description: "Interprets a plain date/time serial as a wall-clock reading in a zone"
seoTitle: "TZLOCALIZE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZLOCALIZE` — conformant with Google Sheets and Excel. Interprets a plain date/time serial as a wall-clock reading in a zone"
llmsDescription: "Use TZLOCALIZE(date_serial,zone) to stamp an existing plain date/time serial (e.g. TODAY() or a numeric cell) with an IANA zone, making it zone-aware. Inverse of TZSERIAL. Optional policy arg handles DST ambiguity. Compose with TZCONVERT to shift zon"
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZLOCALIZE instead of TZDATETIME?","answer":"Use TZLOCALIZE when you already have a date serial in a cell or from a function like TODAY() and want to stamp a timezone onto it without reconstructing year, month, day, hour, minute, and second separately. Use TZDATETIME when you are building a zoned datetime from individual date components."},{"question":"What happens if I pass a serial that was originally produced in a different timezone?","answer":"TZLOCALIZE treats the serial's encoded wall-clock reading as if it were always in the zone you specify. It does not shift the time — it reinterprets it. If the serial actually came from a different zone, use TZCONVERT after localizing to shift it to the correct target zone."},{"question":"When should I pair TZLOCALIZE with TZCONVERT?","answer":"Use TZLOCALIZE first to make a plain serial zone-aware, then chain TZCONVERT to translate it to another zone. For example, localize a serial as US/Eastern and convert to UTC before storing or comparing with other timestamps."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZLOCALIZE(45750, \"America/New_York\")"} />

## When to use it

### When should I use TZLOCALIZE instead of TZDATETIME?

Use TZLOCALIZE when you already have a date serial in a cell or from a function like TODAY() and want to stamp a timezone onto it without reconstructing year, month, day, hour, minute, and second separately. Use TZDATETIME when you are building a zoned datetime from individual date components.

### What happens if I pass a serial that was originally produced in a different timezone?

TZLOCALIZE treats the serial's encoded wall-clock reading as if it were always in the zone you specify. It does not shift the time — it reinterprets it. If the serial actually came from a different zone, use TZCONVERT after localizing to shift it to the correct target zone.

### When should I pair TZLOCALIZE with TZCONVERT?

Use TZLOCALIZE first to make a plain serial zone-aware, then chain TZCONVERT to translate it to another zone. For example, localize a serial as US/Eastern and convert to UTC before storing or comparing with other timestamps.

## Use cases

**Localize a fixed serial to Tokyo time with strict DST policy**

<TryIt formula={"=TZLOCALIZE(45000, \"Asia/Tokyo\", \"reject\")"} />

**Convert a localized serial to UTC for storage**

<TryIt formula={"=TZCONVERT(TZLOCALIZE(45200.5, \"Europe/London\"), \"UTC\")"} />

## Used in

[operations](/industries/operations) · [finance](/industries/finance) · [hr](/industries/hr)

## Syntax

```
TZLOCALIZE(date_serial,zone,[policy])
```

