---
title: "SCAN"
description: "Returns running accumulation using a LAMBDA"
seoTitle: "SCAN: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `SCAN` — conformant with Google Sheets and Excel. Returns running accumulation using a LAMBDA"
llmsDescription: "TrueCalc SCAN function: Returns running accumulation using a LAMBDA Google Sheets/Excel conformant with fixture-verified behavior."
category: "array"
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=SCAN(0,{1,2,3},LAMBDA(acc,x,acc+x))"} />

## Syntax

```
SCAN(initial_value, array, lambda)
```

## Examples

```formula
=SCAN(0,{1,2,3},LAMBDA(acc,x,acc+x)) // => 1
=SCAN(1,{1,2,3,4},LAMBDA(acc,x,acc*x)) // => 1
=SCAN(0,{3,1,4,1,5},LAMBDA(acc,x,MAX(acc,x))) // => 3
```

