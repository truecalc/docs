---
title: "WRAPROWS"
description: "Wraps a vector into rows of the given length"
seoTitle: "WRAPROWS: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `WRAPROWS` — conformant with Google Sheets and Excel. Wraps a vector into rows of the given length"
llmsDescription: "TrueCalc WRAPROWS function: Wraps a vector into rows of the given length Google Sheets/Excel conformant with fixture-verified behavior."
category: "array"
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=WRAPROWS({1,2,3,4,5,6},2)"} />

## Syntax

```
WRAPROWS(vector, wrap_count, [pad_with])
```

## Examples

```formula
=WRAPROWS({1,2,3,4,5,6},2) // => 1
=WRAPROWS({1,2,3,4,5,6},3) // => 1
=WRAPROWS({1,2,3,4},1) // => 1
```

