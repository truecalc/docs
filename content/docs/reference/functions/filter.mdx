---
title: "FILTER"
description: "Filter an array by a boolean mask"
seoTitle: "FILTER: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `FILTER` — conformant with Google Sheets and Excel. Filter an array by a boolean mask"
llmsDescription: "TrueCalc FILTER function: Filter an array by a boolean mask Google Sheets/Excel conformant with fixture-verified behavior."
category: "filter"
jsonLdFaq: [{"question":"When do I use FILTER instead of IF or manually hiding rows?","answer":"Use FILTER when you need a dynamic sub-array of rows or columns that match a condition, returning only the matching elements automatically without helper columns or manual row hiding."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=FILTER({1,2,3,4,5},{TRUE,FALSE,TRUE,FALSE,TRUE})"} />

## When to use it

### When do I use FILTER instead of IF or manually hiding rows?

Use FILTER when you need a dynamic sub-array of rows or columns that match a condition, returning only the matching elements automatically without helper columns or manual row hiding.

## Used in

[operations](/industries/operations) · [finance](/industries/finance)

## Syntax

```
FILTER(array, include, [if_empty])
```

## Examples

```formula
=FILTER({1,2,3,4,5},{TRUE,FALSE,TRUE,FALSE,TRUE}) // => 1
=FILTER({10,20,30,40},{10,20,30,40}>15) // => 20
=FILTER({1,2,3},{TRUE,TRUE,TRUE}) // => 1
```

