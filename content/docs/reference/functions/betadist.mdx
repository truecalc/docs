---
title: "BETADIST"
description: "Beta distribution CDF (legacy)"
seoTitle: "BETADIST: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `BETADIST` — conformant with Google Sheets and Excel. Beta distribution CDF (legacy)"
llmsDescription: "TrueCalc BETADIST function: Beta distribution CDF (legacy) Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use BETADIST?","answer":"Use BETADIST to evaluate the cumulative beta distribution CDF for a value x given shape parameters alpha and beta, optionally rescaled to a [lo, hi] interval — a legacy equivalent of BETA.DIST with cumulative=TRUE."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=BETADIST(0.3,5,1,-1,1)"} />

## When to use it

### When should I use BETADIST?

Use BETADIST to evaluate the cumulative beta distribution CDF for a value x given shape parameters alpha and beta, optionally rescaled to a [lo, hi] interval — a legacy equivalent of BETA.DIST with cumulative=TRUE.

## Use cases

**Probability that project completion is at most 30% of the way through a scaled range**

<TryIt formula={"=BETADIST(0.3,5,1,-1,1)"} />

## Used in

[science](/industries/science) · [operations](/industries/operations)

## Syntax

```
BETADIST(x,alpha,beta,lo,hi)
```

## Examples

```formula
=BETADIST(0.3,5,1,-1,1) // => 0.11602906249999986
```

