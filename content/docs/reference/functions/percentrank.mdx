---
title: "PERCENTRANK"
description: "Percentile rank (inclusive)"
seoTitle: "PERCENTRANK: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `PERCENTRANK` — conformant with Google Sheets and Excel. Percentile rank (inclusive)"
llmsDescription: "TrueCalc PERCENTRANK function: Percentile rank (inclusive) Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use PERCENTRANK?","answer":"Use PERCENTRANK to find the relative standing of a specific value within a dataset as a percentage — for example, what percentile a student's score falls in, or where a product's price sits relative to competitors. The result is a value from 0 to 1."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=PERCENTRANK({1,2,3,4,5},3)"} />

## When to use it

### When should I use PERCENTRANK?

Use PERCENTRANK to find the relative standing of a specific value within a dataset as a percentage — for example, what percentile a student's score falls in, or where a product's price sits relative to competitors. The result is a value from 0 to 1.

## Use cases

**Percentile rank of value 3 in a 1-5 dataset**

<TryIt formula={"=PERCENTRANK({1,2,3,4,5},3)"} />

## Used in

[education](/industries/education) · [operations](/industries/operations)

## Syntax

```
PERCENTRANK(array,x,[sig])
```

## Examples

```formula
=PERCENTRANK({1,2,3,4,5},1) // => 0
=PERCENTRANK({1,2,3,4,5},3) // => 0.5
=PERCENTRANK({1,2,3,4,5},2,7) // => 0.25
```

