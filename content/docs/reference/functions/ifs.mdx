---
title: "IFS"
description: "First value whose condition is true"
seoTitle: "IFS: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `IFS` — conformant with Google Sheets and Excel. First value whose condition is true"
llmsDescription: "TrueCalc IFS function: First value whose condition is true Google Sheets/Excel conformant with fixture-verified behavior."
category: "logical"
jsonLdFaq: [{"question":"When should I use IFS instead of nested IF statements?","answer":"Use IFS when you have several conditions to test in order; it is flatter and easier to read than nesting many IF functions inside each other."},{"question":"What does IFS return if no condition is true?","answer":"IFS returns an #N/A error if none of the conditions match, so add a final TRUE condition as a catch-all default when you need one."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=IFS(85>=90,\"A\",85>=80,\"B\",85>=70,\"C\")"} />

## When to use it

### When should I use IFS instead of nested IF statements?

Use IFS when you have several conditions to test in order; it is flatter and easier to read than nesting many IF functions inside each other.

### What does IFS return if no condition is true?

IFS returns an #N/A error if none of the conditions match, so add a final TRUE condition as a catch-all default when you need one.

## Use cases

**Pick the first matching branch**

<TryIt formula={"=IFS(1>2,\"a\",2>1,\"b\")"} />

**Use a TRUE catch-all as the default**

<TryIt formula={"=IFS(FALSE,\"x\",TRUE,\"y\")"} />

## Used in

[operations](/industries/operations)

## Syntax

```
IFS(cond1, val1,...)
```

