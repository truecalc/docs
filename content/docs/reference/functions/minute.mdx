---
title: "MINUTE"
description: "Minute component of a time serial number"
seoTitle: "MINUTE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `MINUTE` — conformant with Google Sheets and Excel. Minute component of a time serial number"
llmsDescription: "TrueCalc MINUTE function: Minute component of a time serial number Google Sheets/Excel conformant with fixture-verified behavior."
category: "date"
jsonLdFaq: [{"question":"When should I use MINUTE?","answer":"Use MINUTE to extract the minute component (0–59) from a time serial number or time string, useful when computing elapsed time or grouping events into hourly bins."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=MINUTE(TIME(11,40,59))"} />

## When to use it

### When should I use MINUTE?

Use MINUTE to extract the minute component (0–59) from a time serial number or time string, useful when computing elapsed time or grouping events into hourly bins.

## Used in

[operations](/industries/operations)

## Syntax

```
MINUTE(time)
```

## Examples

```formula
=MINUTE(TIME(11,40,59)) // => 40
=MINUTE(40909.0004) // => 0
=MINUTE("20:49:59") // => 49
```

