---
title: "CHISQ.INV"
description: "Chi-squared inverse CDF"
seoTitle: "CHISQ.INV: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `CHISQ.INV` — conformant with Google Sheets and Excel. Chi-squared inverse CDF"
llmsDescription: "TrueCalc CHISQ.INV function: Chi-squared inverse CDF Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use CHISQ.INV?","answer":"Use CHISQ.INV to find the chi-squared value whose left-tail CDF equals p — the inverse of CHISQ.DIST with cumulative=TRUE, used to compute left-tail critical values for confidence intervals or variance tests."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=CHISQ.INV(0.95,4)"} />

## When to use it

### When should I use CHISQ.INV?

Use CHISQ.INV to find the chi-squared value whose left-tail CDF equals p — the inverse of CHISQ.DIST with cumulative=TRUE, used to compute left-tail critical values for confidence intervals or variance tests.

## Used in

[science](/industries/science) · [operations](/industries/operations)

## Syntax

```
CHISQ.INV(p,df)
```

## Examples

```formula
=CHISQ.INV(0.95,4) // => 9.487729036781152
```

