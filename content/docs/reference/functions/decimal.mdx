---
title: "DECIMAL"
description: "Convert string in given base to decimal"
seoTitle: "DECIMAL: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `DECIMAL` — conformant with Google Sheets and Excel. Convert string in given base to decimal"
llmsDescription: "TrueCalc DECIMAL function: Convert string in given base to decimal Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use DECIMAL?","answer":"Use DECIMAL to convert a number string expressed in a given base (e.g. binary or hex) back into a base-10 decimal integer."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=DECIMAL(\"101\",2)"} />

## When to use it

### When should I use DECIMAL?

Use DECIMAL to convert a number string expressed in a given base (e.g. binary or hex) back into a base-10 decimal integer.

## Used in

[engineering](/industries/engineering) · [operations](/industries/operations)

## Syntax

```
DECIMAL(text, base)
```

## Examples

```formula
=DECIMAL("101",2) // => 5
```

