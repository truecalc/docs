---
title: "LARGE"
description: "k-th largest value"
seoTitle: "LARGE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `LARGE` — conformant with Google Sheets and Excel. k-th largest value"
llmsDescription: "TrueCalc LARGE function: k-th largest value Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use LARGE instead of MAX?","answer":"MAX gives only the single highest value. LARGE returns the k-th largest, so LARGE(range, 1) equals MAX, LARGE(range, 2) is the second highest, and so on. Use it for top-N reporting."},{"question":"How do I find the top three values and add them?","answer":"Sum LARGE called for each rank: LARGE(range,1) plus LARGE(range,2) plus LARGE(range,3) gives the total of the three highest values."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=LARGE({3,1,4,1,5}, 2)"} />

## When to use it

### When should I use LARGE instead of MAX?

MAX gives only the single highest value. LARGE returns the k-th largest, so LARGE(range, 1) equals MAX, LARGE(range, 2) is the second highest, and so on. Use it for top-N reporting.

### How do I find the top three values and add them?

Sum LARGE called for each rank: LARGE(range,1) plus LARGE(range,2) plus LARGE(range,3) gives the total of the three highest values.

## Use cases

**Top score from a list of grades**

<TryIt formula={"=LARGE({88, 92, 75, 99, 84}, 1)"} />

**Third-largest deal in a sales pipeline**

<TryIt formula={"=LARGE({120, 340, 90, 560, 230}, 3)"} />

## Used in

[finance](/industries/finance) · [education](/industries/education) · [operations](/industries/operations)

## Syntax

```
LARGE(array,k)
```

## Examples

```formula
=LARGE({3,5,3,5,4,4,2,4,6,7},4) // => 5
```

