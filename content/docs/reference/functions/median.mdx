---
title: "MEDIAN"
description: "Median value"
seoTitle: "MEDIAN: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `MEDIAN` — conformant with Google Sheets and Excel. Median value"
llmsDescription: "TrueCalc MEDIAN function: Median value Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use MEDIAN instead of AVERAGE?","answer":"Use MEDIAN when a few extreme values would distort the mean. For skewed data like salaries, home prices, or response times, the median better reflects the typical value because outliers do not pull it."},{"question":"How does MEDIAN handle an even count of numbers?","answer":"With an even count there is no single middle value, so MEDIAN returns the average of the two middle numbers after sorting."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=MEDIAN(1, 2, 3, 4, 5)"} />

## When to use it

### When should I use MEDIAN instead of AVERAGE?

Use MEDIAN when a few extreme values would distort the mean. For skewed data like salaries, home prices, or response times, the median better reflects the typical value because outliers do not pull it.

### How does MEDIAN handle an even count of numbers?

With an even count there is no single middle value, so MEDIAN returns the average of the two middle numbers after sorting.

## Use cases

**Middle value of a small set**

<TryIt formula={"=MEDIAN(1, 2, 3, 4, 5)"} />

**Typical salary unaffected by one outlier**

<TryIt formula={"=MEDIAN({45000, 52000, 48000, 61000, 250000})"} />

**Median of an even-count dataset**

<TryIt formula={"=MEDIAN(3, 7, 8, 5, 12, 14, 21, 13, 18)"} />

## Used in

[finance](/industries/finance) · [hr](/industries/hr) · [operations](/industries/operations) · [science](/industries/science)

## Syntax

```
MEDIAN(value1,...)
```

## Examples

```formula
=MEDIAN({1,2,3,4,5}) // => 3
=MEDIAN({1,2,3,4}) // => 2.5
=MEDIAN({1,2,3,4,5},26,4) // => 4
```

