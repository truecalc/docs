---
title: "MATCH"
description: "Returns 1-based position of first match"
seoTitle: "MATCH: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `MATCH` — conformant with Google Sheets and Excel. Returns 1-based position of first match"
llmsDescription: "TrueCalc MATCH function: Returns 1-based position of first match Google Sheets/Excel conformant with fixture-verified behavior."
category: "lookup"
jsonLdFaq: [{"question":"When should I use MATCH instead of VLOOKUP?","answer":"Use MATCH when you need the position (row or column number) of a value rather than the value itself — often combined with INDEX to build a flexible lookup."},{"question":"How do I force MATCH to find an exact match?","answer":"Pass 0 as the third argument. Using 1 or -1 assumes the list is sorted and returns the nearest match instead."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=MATCH(20, {10, 20, 30}, 0)"} />

## When to use it

### When should I use MATCH instead of VLOOKUP?

Use MATCH when you need the position (row or column number) of a value rather than the value itself — often combined with INDEX to build a flexible lookup.

### How do I force MATCH to find an exact match?

Pass 0 as the third argument. Using 1 or -1 assumes the list is sorted and returns the nearest match instead.

## Use cases

**Locate a name in a roster**

<TryIt formula={"=MATCH(\"Berlin\", {\"Paris\", \"Berlin\", \"Rome\"}, 0)"} />

**Use MATCH inside INDEX for a lookup**

<TryIt formula={"=INDEX({\"x\", \"y\", \"z\"}, MATCH(30, {10, 20, 30}, 0))"} />

## Used in

[operations](/industries/operations) · [sales](/industries/sales)

## Syntax

```
MATCH(search_key, range, [match_type])
```

## Examples

```formula
=MATCH("a",{"a","b","c"},0) // => 1
=MATCH("b",{"a","b","c"},0) // => 2
=MATCH("c",{"a","b","c"},0) // => 3
```

