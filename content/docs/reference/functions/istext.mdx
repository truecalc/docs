---
title: "ISTEXT"
description: "True if value is text"
seoTitle: "ISTEXT: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `ISTEXT` — conformant with Google Sheets and Excel. True if value is text"
llmsDescription: "TrueCalc ISTEXT function: True if value is text Google Sheets/Excel conformant with fixture-verified behavior."
category: "logical"
jsonLdFaq: [{"question":"When should I use ISTEXT?","answer":"Use ISTEXT to test whether a cell holds text rather than a number, often inside an IF to validate that an entry is a label and not a value."},{"question":"Does ISTEXT return TRUE for a number?","answer":"No — ISTEXT returns FALSE for numbers and TRUE only for text, so it pairs well with ISNUMBER to classify mixed data."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=ISTEXT(\"hello\")"} />

## When to use it

### When should I use ISTEXT?

Use ISTEXT to test whether a cell holds text rather than a number, often inside an IF to validate that an entry is a label and not a value.

### Does ISTEXT return TRUE for a number?

No — ISTEXT returns FALSE for numbers and TRUE only for text, so it pairs well with ISNUMBER to classify mixed data.

## Use cases

**Confirm a value is text**

<TryIt formula={"=ISTEXT(\"hello\")"} />

**A number is not text**

<TryIt formula={"=ISTEXT(123)"} />

**Check a single character is text**

<TryIt formula={"=ISTEXT(\"x\")"} />

## Used in

[operations](/industries/operations) · [education](/industries/education) · [finance](/industries/finance) · [engineering](/industries/engineering)

## Syntax

```
ISTEXT(value)
```

