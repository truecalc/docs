---
title: "SWITCH"
description: "Match expression against values"
seoTitle: "SWITCH: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `SWITCH` — conformant with Google Sheets and Excel. Match expression against values"
llmsDescription: "TrueCalc SWITCH function: Match expression against values Google Sheets/Excel conformant with fixture-verified behavior."
category: "logical"
jsonLdFaq: [{"question":"When should I use SWITCH instead of IFS?","answer":"Use SWITCH when you are comparing one expression against several exact values; use IFS when each branch needs a different logical test."},{"question":"Can SWITCH have a default value?","answer":"Yes — a final argument with no matching value acts as the default that SWITCH returns when nothing else matches."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=SWITCH(2,1,\"one\",2,\"two\",\"other\")"} />

## When to use it

### When should I use SWITCH instead of IFS?

Use SWITCH when you are comparing one expression against several exact values; use IFS when each branch needs a different logical test.

### Can SWITCH have a default value?

Yes — a final argument with no matching value acts as the default that SWITCH returns when nothing else matches.

## Use cases

**Map a number to a label with a default**

<TryIt formula={"=SWITCH(2,1,\"one\",2,\"two\",\"other\")"} />

**Convert a day number to a name**

<TryIt formula={"=SWITCH(3,1,\"Mon\",2,\"Tue\",\"Other\")"} />

**Look up a code and return its value**

<TryIt formula={"=SWITCH(\"B\",\"A\",1,\"B\",2,0)"} />

## Used in

[operations](/industries/operations) · [education](/industries/education) · [hr](/industries/hr) · [retail](/industries/retail) · [sales](/industries/sales)

## Syntax

```
SWITCH(expr, val1, result1,...)
```

