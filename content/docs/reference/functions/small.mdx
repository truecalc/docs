---
title: "SMALL"
description: "k-th smallest value"
seoTitle: "SMALL: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `SMALL` — conformant with Google Sheets and Excel. k-th smallest value"
llmsDescription: "TrueCalc SMALL function: k-th smallest value Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use SMALL instead of MIN?","answer":"MIN gives only the single lowest value. SMALL returns the k-th smallest, so SMALL(range, 1) equals MIN, SMALL(range, 2) is the second lowest, and so on. Use it for bottom-N reporting."},{"question":"How do I find the lowest few values?","answer":"Call SMALL with rank 1, 2, 3, and so on. SMALL(range, 2) returns the second smallest value, which is useful when you want to skip a single extreme low."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=SMALL({3,1,4,1,5}, 2)"} />

## When to use it

### When should I use SMALL instead of MIN?

MIN gives only the single lowest value. SMALL returns the k-th smallest, so SMALL(range, 1) equals MIN, SMALL(range, 2) is the second lowest, and so on. Use it for bottom-N reporting.

### How do I find the lowest few values?

Call SMALL with rank 1, 2, 3, and so on. SMALL(range, 2) returns the second smallest value, which is useful when you want to skip a single extreme low.

## Use cases

**Second-lowest value in a set**

<TryIt formula={"=SMALL({3,1,4,1,5}, 2)"} />

**Lowest score from a list of grades**

<TryIt formula={"=SMALL({88, 92, 75, 99, 84}, 1)"} />

**Second-cheapest measured value**

<TryIt formula={"=SMALL({4.5, 2.1, 6.8, 3.3}, 2)"} />

## Used in

[retail](/industries/retail) · [operations](/industries/operations) · [education](/industries/education) · [science](/industries/science)

## Syntax

```
SMALL(array,k)
```

## Examples

```formula
=SMALL({3,1,4,1,5,9,2,6},4) // => 3
=SMALL({3,1,4,1,5,9,2,6},1) // => 1
=SMALL({3,1,4,1,5,9,2,6},8) // => 9
```

