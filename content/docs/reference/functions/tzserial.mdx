---
title: "TZSERIAL"
description: "Drops the zone, returning the wall-clock time as a plain date serial"
seoTitle: "TZSERIAL: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZSERIAL` — conformant with Google Sheets and Excel. Drops the zone, returning the wall-clock time as a plain date serial"
llmsDescription: "Use TZSERIAL(zoned) to strip the zone from a zone-aware instant and return the local wall-clock time as a plain date/time serial. Lets you feed zone-aware values into ordinary functions like YEAR(), HOUR(), or NETWORKDAYS(). Inverse of TZLOCALIZE."
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZSERIAL instead of reading the value directly from TZDATETIME?","answer":"Use TZSERIAL when you need to pass a zone-aware value into ordinary date/time functions like YEAR(), MONTH(), HOUR(), or MINUTE() that only accept plain serial numbers, not zoned values."},{"question":"What is the difference between TZSERIAL and TZSTRING?","answer":"TZSERIAL drops the zone and returns the wall-clock time as a numeric serial number suitable for arithmetic and built-in date functions, while TZSTRING returns a human-readable text representation of the zoned datetime."},{"question":"What happens to the timezone offset when I call TZSERIAL?","answer":"The offset is discarded entirely. TZSERIAL returns the local wall-clock time as-is, so 09:30 in Tokyo and 09:30 in London both return the same serial number representing 09:30."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=HOUR(TZSERIAL(TZDATETIME(2024, 6, 15, 9, 30, 0, \"America/New_York\")))"} />

## When to use it

### When should I use TZSERIAL instead of reading the value directly from TZDATETIME?

Use TZSERIAL when you need to pass a zone-aware value into ordinary date/time functions like YEAR(), MONTH(), HOUR(), or MINUTE() that only accept plain serial numbers, not zoned values.

### What is the difference between TZSERIAL and TZSTRING?

TZSERIAL drops the zone and returns the wall-clock time as a numeric serial number suitable for arithmetic and built-in date functions, while TZSTRING returns a human-readable text representation of the zoned datetime.

### What happens to the timezone offset when I call TZSERIAL?

The offset is discarded entirely. TZSERIAL returns the local wall-clock time as-is, so 09:30 in Tokyo and 09:30 in London both return the same serial number representing 09:30.

## Use cases

**Get the day of the month from a zoned value**

<TryIt formula={"=DAY(TZSERIAL(TZDATETIME(2024, 3, 27, 14, 0, 0, \"Europe/London\")))"} />

**Calculate days remaining in the month using a zoned datetime**

<TryIt formula={"=EOMONTH(TZSERIAL(TZDATETIME(2024, 11, 10, 8, 0, 0, \"Asia/Tokyo\")), 0) - TZSERIAL(TZDATETIME(2024, 11, 10, 8, 0, 0, \"Asia/Tokyo\"))"} />

## Used in

[operations](/industries/operations) · [finance](/industries/finance) · [engineering](/industries/engineering)

## Syntax

```
TZSERIAL(zoned)
```

