---
title: "TZDBVERSION"
description: "IANA time-zone database version the engine is using"
seoTitle: "TZDBVERSION: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZDBVERSION` — conformant with Google Sheets and Excel. IANA time-zone database version the engine is using"
llmsDescription: "Use TZDBVERSION() to get the IANA timezone database version compiled into the TrueCalc engine, e.g. \"2025b\". No arguments. Use in CI assertions to verify the tzdb version, or in compliance reports to document which DST rules were in effect when a cal"
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZDBVERSION instead of hardcoding the expected tzdb version?","answer":"Use TZDBVERSION in CI assertions and compliance checks so the formula automatically adapts when the engine is updated, rather than requiring a manual string update each time."},{"question":"What does TZDBVERSION return, and how is it formatted?","answer":"It returns a string like \"2025b\" — the IANA time-zone database release tag the engine was compiled with, consisting of a four-digit year followed by a lowercase letter."},{"question":"What happens if TZDBVERSION returns an older version than expected?","answer":"It means the engine binary has not yet been updated to the latest tzdb release; time-zone rules for recently changed regions may be stale, which can affect TZCONVERT and TZDATETIME results."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZDBVERSION()"} />

## When to use it

### When should I use TZDBVERSION instead of hardcoding the expected tzdb version?

Use TZDBVERSION in CI assertions and compliance checks so the formula automatically adapts when the engine is updated, rather than requiring a manual string update each time.

### What does TZDBVERSION return, and how is it formatted?

It returns a string like "2025b" — the IANA time-zone database release tag the engine was compiled with, consisting of a four-digit year followed by a lowercase letter.

### What happens if TZDBVERSION returns an older version than expected?

It means the engine binary has not yet been updated to the latest tzdb release; time-zone rules for recently changed regions may be stale, which can affect TZCONVERT and TZDATETIME results.

## Use cases

**Check whether the engine meets a minimum tzdb version**

<TryIt formula={"=EXACT(TZDBVERSION(), \"2025b\")"} />

**Validate tzdb version before a timezone conversion**

<TryIt formula={"=IF(EXACT(TZDBVERSION(), \"2025b\"), TZSTRING(TZCONVERT(TZDATETIME(2025, 7, 4, 12, 0, 0, \"America/New_York\"), \"UTC\")), \"tzdb version mismatch\")"} />

## Used in

[engineering](/industries/engineering)

## Syntax

```
TZDBVERSION()
```

