---
title: "COUNTBLANK"
description: "Count blank/empty cells"
seoTitle: "COUNTBLANK: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `COUNTBLANK` — conformant with Google Sheets and Excel. Count blank/empty cells"
llmsDescription: "TrueCalc COUNTBLANK function: Count blank/empty cells Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use COUNTBLANK instead of COUNTA?","answer":"Use COUNTBLANK to count the empty cells in a range; use COUNTA to count the cells that contain something. They are complements of each other."},{"question":"Does COUNTBLANK count cells that hold an empty string?","answer":"Yes — a cell holding an empty string (\"\") is treated as blank by COUNTBLANK, even though it is not truly empty."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=COUNTBLANK({1,\"\",3})"} />

## When to use it

### When should I use COUNTBLANK instead of COUNTA?

Use COUNTBLANK to count the empty cells in a range; use COUNTA to count the cells that contain something. They are complements of each other.

### Does COUNTBLANK count cells that hold an empty string?

Yes — a cell holding an empty string ("") is treated as blank by COUNTBLANK, even though it is not truly empty.

## Use cases

**Find how many cells in a column are still blank**

<TryIt formula={"=COUNTBLANK({1;\"\";3})"} />

**Count missing values where most entries are empty**

<TryIt formula={"=COUNTBLANK({\"a\",\"\",\"\"})"} />

## Used in

[operations](/industries/operations) · [education](/industries/education)

## Syntax

```
COUNTBLANK(range)
```

## Examples

```formula
=COUNTBLANK({1,"",3,"",5}) // => 2
=COUNTBLANK({"","",""}) // => 3
=COUNTBLANK({1,2,3}) // => 0
```

