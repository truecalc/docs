---
title: "WRAPCOLS"
description: "Wraps a vector into columns of the given length"
seoTitle: "WRAPCOLS: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `WRAPCOLS` — conformant with Google Sheets and Excel. Wraps a vector into columns of the given length"
llmsDescription: "TrueCalc WRAPCOLS function: Wraps a vector into columns of the given length Google Sheets/Excel conformant with fixture-verified behavior."
category: "array"
jsonLdFaq: [{"question":"When should I use WRAPCOLS?","answer":"Use WRAPCOLS to reshape a flat vector into a multi-column array of a fixed column height — useful for converting a sequential list into a grid layout for display or further array operations."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=WRAPCOLS({1,2,3,4,5,6},2)"} />

## When to use it

### When should I use WRAPCOLS?

Use WRAPCOLS to reshape a flat vector into a multi-column array of a fixed column height — useful for converting a sequential list into a grid layout for display or further array operations.

## Used in

[operations](/industries/operations)

## Syntax

```
WRAPCOLS(vector, wrap_count, [pad_with])
```

## Examples

```formula
=WRAPCOLS({1,2,3,4,5,6},2) // => 1
=WRAPCOLS({1,2,3,4,5,6},3) // => 1
=WRAPCOLS({1,2,3,4},1) // => 1
```

