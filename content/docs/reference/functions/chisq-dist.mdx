---
title: "CHISQ.DIST"
description: "Chi-squared CDF/PDF"
seoTitle: "CHISQ.DIST: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `CHISQ.DIST` — conformant with Google Sheets and Excel. Chi-squared CDF/PDF"
llmsDescription: "TrueCalc CHISQ.DIST function: Chi-squared CDF/PDF Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use CHISQ.DIST?","answer":"Use CHISQ.DIST to compute either the chi-squared PDF (cumulative=FALSE) or left-tail CDF (cumulative=TRUE) for a given value and degrees of freedom — the modern replacement for CHIDIST when you need the left tail or density."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=CHISQ.DIST(12.3,5,FALSE)"} />

## When to use it

### When should I use CHISQ.DIST?

Use CHISQ.DIST to compute either the chi-squared PDF (cumulative=FALSE) or left-tail CDF (cumulative=TRUE) for a given value and degrees of freedom — the modern replacement for CHIDIST when you need the left tail or density.

## Used in

[science](/industries/science) · [operations](/industries/operations)

## Syntax

```
CHISQ.DIST(x,df,cumulative)
```

## Examples

```formula
=CHISQ.DIST(12.3,5,FALSE) // => 0.012238703529441487
```

