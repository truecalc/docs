---
title: "TRIM"
description: "Remove extra whitespace"
seoTitle: "TRIM: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TRIM` — conformant with Google Sheets and Excel. Remove extra whitespace"
llmsDescription: "TrueCalc TRIM function: Remove extra whitespace Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"What does TRIM remove?","answer":"TRIM strips leading and trailing spaces and collapses repeated internal spaces down to a single space. It does not remove other whitespace such as line breaks."},{"question":"Why do my lookups fail even when the text looks identical?","answer":"Stray spaces from imports or copy-paste are a common cause. Wrap both sides in TRIM so an extra trailing space does not break an exact match."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TRIM(\"  Jane   Doe  \")"} />

## When to use it

### What does TRIM remove?

TRIM strips leading and trailing spaces and collapses repeated internal spaces down to a single space. It does not remove other whitespace such as line breaks.

### Why do my lookups fail even when the text looks identical?

Stray spaces from imports or copy-paste are a common cause. Wrap both sides in TRIM so an extra trailing space does not break an exact match.

## Use cases

**Normalize a product label before matching**

<TryIt formula={"=TRIM(\"  Red Widget \")"} />

**Strip padding from a code**

<TryIt formula={"=TRIM(\"  ABC-123  \")"} />

## Used in

[operations](/industries/operations) · [retail](/industries/retail)

## Syntax

```
TRIM(text)
```

