---
title: "MAX"
description: "Maximum value"
seoTitle: "MAX: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `MAX` — conformant with Google Sheets and Excel. Maximum value"
llmsDescription: "TrueCalc MAX function: Maximum value Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use MAX instead of LARGE?","answer":"Use MAX for the single highest value; use LARGE when you need the second-highest, third-highest, and so on."},{"question":"Does MAX ignore text and blanks?","answer":"Yes. MAX considers only numeric values and skips text and empty cells."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=MAX(3, 7, 2)"} />

## When to use it

### When should I use MAX instead of LARGE?

Use MAX for the single highest value; use LARGE when you need the second-highest, third-highest, and so on.

### Does MAX ignore text and blanks?

Yes. MAX considers only numeric values and skips text and empty cells.

## Use cases

**Find the highest of several numbers**

<TryIt formula={"=MAX(3, 7, 2)"} />

**Get the peak value from a list**

<TryIt formula={"=MAX({12, 45, 23, 8})"} />

**Identify the top revenue figure**

<TryIt formula={"=MAX(1500, 2300, 1800, 2100)"} />

## Used in

[operations](/industries/operations) · [sales](/industries/sales) · [science](/industries/science) · [finance](/industries/finance)

## Syntax

```
MAX(value1,...)
```

## Examples

```formula
=MAX(PRICES) // => 30
=MAX(1,2,3,4,5) // => 5
=MAX({1,2,3},{4,5,6},4,26) // => 26
```

