---
title: "REGEXREPLACE"
description: "Replace regex matches in text"
seoTitle: "REGEXREPLACE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `REGEXREPLACE` — conformant with Google Sheets and Excel. Replace regex matches in text"
llmsDescription: "TrueCalc REGEXREPLACE function: Replace regex matches in text Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use REGEXREPLACE instead of SUBSTITUTE?","answer":"Use REGEXREPLACE when the text you want to replace follows a pattern rather than a fixed string, such as removing all punctuation or normalizing whitespace."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=REGEXREPLACE(\"Order 1234\", \"[0-9]+\", \"\")"} />

## When to use it

### When should I use REGEXREPLACE instead of SUBSTITUTE?

Use REGEXREPLACE when the text you want to replace follows a pattern rather than a fixed string, such as removing all punctuation or normalizing whitespace.

## Use cases

**Remove all digits from a string**

<TryIt formula={"=REGEXREPLACE(\"Order 1234\", \"[0-9]+\", \"\")"} />

## Used in

[operations](/industries/operations) · [sales](/industries/sales)

## Syntax

```
REGEXREPLACE(text, pattern, replacement)
```

