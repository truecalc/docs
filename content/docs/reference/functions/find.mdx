---
title: "FIND"
description: "Case-sensitive position search"
seoTitle: "FIND: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `FIND` — conformant with Google Sheets and Excel. Case-sensitive position search"
llmsDescription: "TrueCalc FIND function: Case-sensitive position search Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use FIND instead of SEARCH?","answer":"Use FIND for a case-sensitive match that does not allow wildcards; use SEARCH when you want a case-insensitive match or wildcard support."},{"question":"What does FIND return if the text is not present?","answer":"FIND returns a #VALUE! error when the search text is not found, so wrap it in IFERROR if a missing match is expected."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=FIND(\"-\",\"US-12345\")"} />

## When to use it

### When should I use FIND instead of SEARCH?

Use FIND for a case-sensitive match that does not allow wildcards; use SEARCH when you want a case-insensitive match or wildcard support.

### What does FIND return if the text is not present?

FIND returns a #VALUE! error when the search text is not found, so wrap it in IFERROR if a missing match is expected.

## Use cases

**Find the position of the @ in an email address**

<TryIt formula={"=FIND(\"@\",\"name@example.com\")"} />

**Find a letter inside a short string**

<TryIt formula={"=FIND(\"b\",\"abc\")"} />

## Used in

[operations](/industries/operations) · [retail](/industries/retail) · [sales](/industries/sales)

## Syntax

```
FIND(find_text, within_text, start)
```

## Examples

```formula
=FIND("n","printing") // => 4
=FIND("wood","How much wood can a woodchuck chuck",14) // => 21
=FIND("H","Hello World") // => 1
```

