---
title: "XIRR"
description: "IRR for irregular cash flows"
seoTitle: "XIRR: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `XIRR` — conformant with Google Sheets and Excel. IRR for irregular cash flows"
llmsDescription: "TrueCalc XIRR function: IRR for irregular cash flows Google Sheets/Excel conformant with fixture-verified behavior."
category: "financial"
jsonLdFaq: [{"question":"When should I use XIRR instead of IRR?","answer":"Use XIRR when your cash flows occur on irregular (non-periodic) dates — XIRR takes an explicit dates array so each cash flow is discounted by its exact time distance, whereas IRR assumes equally spaced periods."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=XIRR({-4000,200,250,300},{DATE(2012,1,1),DATE(2012,6,23),DATE(2013,5,12),DATE(2014,2,9)},0.09)"} />

## When to use it

### When should I use XIRR instead of IRR?

Use XIRR when your cash flows occur on irregular (non-periodic) dates — XIRR takes an explicit dates array so each cash flow is discounted by its exact time distance, whereas IRR assumes equally spaced periods.

## Used in

[finance](/industries/finance)

## Syntax

```
XIRR(values, dates, [guess])
```

## Examples

```formula
=XIRR({-4000,200,250,300},{DATE(2012,1,1),DATE(2012,6,23),DATE(2013,5,12),DATE(2014,2,9)},0.09) // => -0.6440855342116852
```

