---
title: "WEEKDAY"
description: "Day of week as a number"
seoTitle: "WEEKDAY: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `WEEKDAY` — conformant with Google Sheets and Excel. Day of week as a number"
llmsDescription: "TrueCalc WEEKDAY function: Day of week as a number Google Sheets/Excel conformant with fixture-verified behavior."
category: "date"
jsonLdFaq: [{"question":"When should I use WEEKDAY instead of DAY?","answer":"Use WEEKDAY to get the day of the week (1-7) from a date; use DAY for the day-of-month number."},{"question":"What does the second argument of WEEKDAY control?","answer":"It sets which day counts as 1. The default (1) starts the week on Sunday; type 2 starts it on Monday."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=WEEKDAY(DATE(2024, 3, 15))"} />

## When to use it

### When should I use WEEKDAY instead of DAY?

Use WEEKDAY to get the day of the week (1-7) from a date; use DAY for the day-of-month number.

### What does the second argument of WEEKDAY control?

It sets which day counts as 1. The default (1) starts the week on Sunday; type 2 starts it on Monday.

## Use cases

**Get the weekday number for a date**

<TryIt formula={"=WEEKDAY(DATE(2024, 3, 15))"} />

**Use Monday-start numbering for scheduling**

<TryIt formula={"=WEEKDAY(DATE(2024, 3, 15), 2)"} />

**Flag whether a date is a weekend**

<TryIt formula={"=IF(WEEKDAY(DATE(2024, 3, 16), 2) > 5, \"weekend\", \"weekday\")"} />

## Used in

[operations](/industries/operations) · [hr](/industries/hr) · [retail](/industries/retail)

## Syntax

```
WEEKDAY(date,[type])
```

## Examples

```formula
=WEEKDAY(DATE(1969,7,20)) // => 1
=WEEKDAY(40909) // => 1
=WEEKDAY(40909,3) // => 6
```

