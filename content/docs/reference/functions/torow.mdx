---
title: "TOROW"
description: "Converts an array to a single row"
seoTitle: "TOROW: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TOROW` — conformant with Google Sheets and Excel. Converts an array to a single row"
llmsDescription: "TrueCalc TOROW function: Converts an array to a single row Google Sheets/Excel conformant with fixture-verified behavior."
category: "array"
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TOROW({1,2;3,4})"} />

## Syntax

```
TOROW(array, [ignore], [scan_by_col])
```

## Examples

```formula
=TOROW({1,2;3,4}) // => 1
=TOROW({1;2;3}) // => 1
=TOROW({1,2,3;4,5,6;7,8,9}) // => 1
```

