---
title: "FINDB"
description: "Case-sensitive byte-position search"
seoTitle: "FINDB: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `FINDB` — conformant with Google Sheets and Excel. Case-sensitive byte-position search"
llmsDescription: "TrueCalc FINDB function: Case-sensitive byte-position search Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use FINDB instead of FIND?","answer":"Use FINDB when working with multibyte character sets (CJK text) and you need a byte-position rather than a character-position, or when conforming to byte-indexed string operations."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=FINDB(\"n\", \"printing\")"} />

## When to use it

### When should I use FINDB instead of FIND?

Use FINDB when working with multibyte character sets (CJK text) and you need a byte-position rather than a character-position, or when conforming to byte-indexed string operations.

## Use cases

**Find byte position of a character in ASCII text**

<TryIt formula={"=FINDB(\"n\", \"printing\")"} />

## Used in

[operations](/industries/operations)

## Syntax

```
FINDB(find_text, within_text, [start_num])
```

## Examples

```formula
=FINDB("新","农历新年") // => 5
=FINDB("新","农历新年",2) // => 5
=FINDB("n","printing") // => 4
```

