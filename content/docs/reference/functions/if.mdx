---
title: "IF"
description: "Conditional value"
seoTitle: "IF: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `IF` — conformant with Google Sheets and Excel. Conditional value"
llmsDescription: "TrueCalc IF function: Conditional value Google Sheets/Excel conformant with fixture-verified behavior."
category: "logical"
jsonLdFaq: [{"question":"When should I use IF instead of IFERROR?","answer":"Use IF to choose between two outcomes based on a condition you write; use IFERROR only to catch and replace a formula that returns an error."},{"question":"How do I check more than one condition with IF?","answer":"Nest IF statements or combine them with AND/OR inside the test, e.g. IF(AND(x>0, y>0), both, not both)."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=IF(5 > 3, \"yes\", \"no\")"} />

## When to use it

### When should I use IF instead of IFERROR?

Use IF to choose between two outcomes based on a condition you write; use IFERROR only to catch and replace a formula that returns an error.

### How do I check more than one condition with IF?

Nest IF statements or combine them with AND/OR inside the test, e.g. IF(AND(x&gt;0, y&gt;0), both, not both).

## Use cases

**Label a value as high or low**

<TryIt formula={"=IF(5 > 3, \"yes\", \"no\")"} />

**Pass or fail based on a score**

<TryIt formula={"=IF(72 >= 60, \"Pass\", \"Fail\")"} />

**Flag whether a budget is over or under**

<TryIt formula={"=IF(950 > 1000, \"Over budget\", \"Within budget\")"} />

## Used in

[finance](/industries/finance) · [operations](/industries/operations) · [education](/industries/education) · [sales](/industries/sales)

## Syntax

```
IF(condition, true_val, false_val)
```

## Examples

```formula
=IF(TRUE,4,5) // => 4
=IF(FALSE,4,5) // => 5
```

