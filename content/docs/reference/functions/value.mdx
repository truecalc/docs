---
title: "VALUE"
description: "Convert text to number"
seoTitle: "VALUE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `VALUE` — conformant with Google Sheets and Excel. Convert text to number"
llmsDescription: "TrueCalc VALUE function: Convert text to number Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use VALUE?","answer":"Use VALUE to convert a number stored as text into a real number so it can be used in calculations."},{"question":"What happens if VALUE gets text that is not a number?","answer":"VALUE returns a #VALUE! error when the text cannot be interpreted as a number, so clean or validate the input first if it may contain letters."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=VALUE(\"123\")"} />

## When to use it

### When should I use VALUE?

Use VALUE to convert a number stored as text into a real number so it can be used in calculations.

### What happens if VALUE gets text that is not a number?

VALUE returns a #VALUE! error when the text cannot be interpreted as a number, so clean or validate the input first if it may contain letters.

## Use cases

**Convert a number stored as text**

<TryIt formula={"=VALUE(\"123\")"} />

**Turn an imported numeric string into a number**

<TryIt formula={"=VALUE(\"1500\")"} />

**Coerce a text digit so it can be summed**

<TryIt formula={"=VALUE(\"42\")"} />

## Used in

[finance](/industries/finance) · [operations](/industries/operations) · [retail](/industries/retail) · [education](/industries/education)

## Syntax

```
VALUE(text)
```

## Examples

```formula
=VALUE("123") // => 123
=VALUE("7/20/1969") // => 25404
=VALUE("12:00:00") // => 0.5
```

