---
title: "GESTEP"
description: "Test whether a number is greater than or equal to a step value"
seoTitle: "GESTEP: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `GESTEP` — conformant with Google Sheets and Excel. Test whether a number is greater than or equal to a step value"
llmsDescription: "TrueCalc GESTEP function: Test whether a number is greater than or equal to a step value Google Sheets/Excel conformant with fixture-verified behavior."
category: "engineering"
jsonLdFaq: [{"question":"When should I use GESTEP?","answer":"Use GESTEP to test whether a number is greater than or equal to a threshold, returning 1 (true) or 0 (false) — useful for counting values that meet or exceed a step without an IF formula."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=GESTEP(5,2)"} />

## When to use it

### When should I use GESTEP?

Use GESTEP to test whether a number is greater than or equal to a threshold, returning 1 (true) or 0 (false) — useful for counting values that meet or exceed a step without an IF formula.

## Use cases

**Test whether a value meets a threshold**

<TryIt formula={"=GESTEP(5,2)"} />

## Used in

[engineering](/industries/engineering) · [science](/industries/science)

## Syntax

```
GESTEP(number, [step])
```

## Examples

```formula
=GESTEP(5,2) // => 1
=GESTEP(2,2) // => 1
=GESTEP(-6,-1) // => 0
```

