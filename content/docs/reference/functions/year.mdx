---
title: "YEAR"
description: "Year from a date serial number"
seoTitle: "YEAR: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `YEAR` — conformant with Google Sheets and Excel. Year from a date serial number"
llmsDescription: "TrueCalc YEAR function: Year from a date serial number Google Sheets/Excel conformant with fixture-verified behavior."
category: "date"
jsonLdFaq: [{"question":"What does YEAR return?","answer":"YEAR takes a date and returns its four-digit year as a number, which you can use in comparisons, grouping, or further calculations."},{"question":"How do I group records by year?","answer":"Apply YEAR to each date to get the year number, then use it as the key in a SUMIF, COUNTIF, or pivot to roll figures up by year."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=YEAR(DATE(2024, 5, 15))"} />

## When to use it

### What does YEAR return?

YEAR takes a date and returns its four-digit year as a number, which you can use in comparisons, grouping, or further calculations.

### How do I group records by year?

Apply YEAR to each date to get the year number, then use it as the key in a SUMIF, COUNTIF, or pivot to roll figures up by year.

## Use cases

**Find the year a contract started**

<TryIt formula={"=YEAR(DATE(2021, 11, 1))"} />

**Extract the reporting year**

<TryIt formula={"=YEAR(DATE(2023, 12, 31))"} />

## Used in

[finance](/industries/finance) · [operations](/industries/operations)

## Syntax

```
YEAR(date)
```

## Examples

```formula
=YEAR(DATE(1969,7,20)) // => 1969
=YEAR(40909) // => 2012
=YEAR(DATE(2020,2,29)) // => 2020
```

