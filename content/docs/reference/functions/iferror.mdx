---
title: "IFERROR"
description: "Return alternate value on error"
seoTitle: "IFERROR: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `IFERROR` — conformant with Google Sheets and Excel. Return alternate value on error"
llmsDescription: "TrueCalc IFERROR function: Return alternate value on error Google Sheets/Excel conformant with fixture-verified behavior."
category: "logical"
jsonLdFaq: [{"question":"When should I use IFERROR instead of IF?","answer":"Use IFERROR to catch and replace a formula that returns an error; use IF when you want to branch on a condition you define yourself."},{"question":"Does IFERROR hide all errors?","answer":"Yes — it replaces any error (like #DIV/0! or #N/A) with the fallback you provide, so use it carefully or you may mask a real mistake."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=IFERROR(1/0, \"safe\")"} />

## When to use it

### When should I use IFERROR instead of IF?

Use IFERROR to catch and replace a formula that returns an error; use IF when you want to branch on a condition you define yourself.

### Does IFERROR hide all errors?

Yes — it replaces any error (like #DIV/0! or #N/A) with the fallback you provide, so use it carefully or you may mask a real mistake.

## Use cases

**Return a friendly message when a lookup fails**

<TryIt formula={"=IFERROR(VLOOKUP(\"z\", {\"a\", 1; \"b\", 2}, 2, FALSE), \"Not found\")"} />

**Default to zero when a calculation errors**

<TryIt formula={"=IFERROR(10 / 0, 0)"} />

## Used in

[operations](/industries/operations)

## Syntax

```
IFERROR(value, value_if_error)
```

## Examples

```formula
=IFERROR(5,0) // => 5
=IFERROR(NA(),0) // => 0
```

