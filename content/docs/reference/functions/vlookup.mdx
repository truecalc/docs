---
title: "VLOOKUP"
description: "Searches first column of range, returns value from index column"
seoTitle: "VLOOKUP: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `VLOOKUP` — conformant with Google Sheets and Excel. Searches first column of range, returns value from index column"
llmsDescription: "TrueCalc VLOOKUP function: Searches first column of range, returns value from index column Google Sheets/Excel conformant with fixture-verified behavior."
category: "lookup"
jsonLdFaq: [{"question":"When should I use VLOOKUP instead of XLOOKUP?","answer":"Use XLOOKUP when it is available — it can look left, returns a clean result when nothing is found, and does not break when columns move. Reach for VLOOKUP mainly for compatibility with older sheets."},{"question":"Why does my VLOOKUP return the wrong value?","answer":"Usually because the last argument is left as approximate match. Pass FALSE as the fourth argument to force an exact match unless your lookup column is sorted and you truly want the nearest value."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=VLOOKUP(\"b\", {\"a\", 1; \"b\", 2; \"c\", 3}, 2, FALSE)"} />

## When to use it

### When should I use VLOOKUP instead of XLOOKUP?

Use XLOOKUP when it is available — it can look left, returns a clean result when nothing is found, and does not break when columns move. Reach for VLOOKUP mainly for compatibility with older sheets.

### Why does my VLOOKUP return the wrong value?

Usually because the last argument is left as approximate match. Pass FALSE as the fourth argument to force an exact match unless your lookup column is sorted and you truly want the nearest value.

## Use cases

**Look up a product code's price**

<TryIt formula={"=VLOOKUP(\"b\", {\"a\", 1; \"b\", 2; \"c\", 3}, 2, FALSE)"} />

**Find an employee's department by ID**

<TryIt formula={"=VLOOKUP(102, {101, \"Sales\"; 102, \"Finance\"; 103, \"Ops\"}, 2, FALSE)"} />

**Map a tier number to a discount rate**

<TryIt formula={"=VLOOKUP(2, {1, 0.05; 2, 0.1; 3, 0.15}, 2, FALSE)"} />

## Used in

[retail](/industries/retail) · [sales](/industries/sales) · [finance](/industries/finance) · [hr](/industries/hr)

## Syntax

```
VLOOKUP(search_key, range, index, [is_sorted])
```

## Examples

```formula
=VLOOKUP("b",{"a",1;"b",2;"c",3},2,FALSE) // => 2
=VLOOKUP("a",{"a",10;"b",20;"c",30},2,FALSE) // => 10
=VLOOKUP("c",{"a",10;"b",20;"c",30},2,FALSE) // => 30
```

