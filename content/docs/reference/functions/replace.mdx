---
title: "REPLACE"
description: "Replace portion of text"
seoTitle: "REPLACE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `REPLACE` — conformant with Google Sheets and Excel. Replace portion of text"
llmsDescription: "TrueCalc REPLACE function: Replace portion of text Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use REPLACE instead of SUBSTITUTE?","answer":"Use REPLACE when you know the position and length of the text to swap out; use SUBSTITUTE when you want to replace text by matching its content."},{"question":"What do the numbers in REPLACE mean?","answer":"The second argument is the starting position and the third is how many characters to remove; the fourth argument is the new text to insert there."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=REPLACE(\"555-1234\",1,3,\"888\")"} />

## When to use it

### When should I use REPLACE instead of SUBSTITUTE?

Use REPLACE when you know the position and length of the text to swap out; use SUBSTITUTE when you want to replace text by matching its content.

### What do the numbers in REPLACE mean?

The second argument is the starting position and the third is how many characters to remove; the fourth argument is the new text to insert there.

## Use cases

**Swap a slice of characters by position**

<TryIt formula={"=REPLACE(\"abcdef\",2,3,\"XY\")"} />

**Rename a fixed-length label**

<TryIt formula={"=REPLACE(\"PRODUCT\",1,7,\"ITEM\")"} />

## Used in

[operations](/industries/operations) · [sales](/industries/sales) · [retail](/industries/retail)

## Syntax

```
REPLACE(text, start, num_chars, new_text)
```

