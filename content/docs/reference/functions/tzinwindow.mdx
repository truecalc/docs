---
title: "TZINWINDOW"
description: "TRUE if the local time-of-day falls in [start,end); days_mask is 7 chars Sun..Sat"
seoTitle: "TZINWINDOW: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZINWINDOW` — conformant with Google Sheets and Excel. TRUE if the local time-of-day falls in [start,end); days_mask is 7 chars Sun..Sat"
llmsDescription: "Use TZINWINDOW(zoned,start,end) to check if a zone-aware instant's local time falls within [start,end). start/end are fractional days (9/24 = 9am, 17/24 = 5pm). Optional 7-char days_mask (e.g. \"_MTWTF_\") restricts to weekdays. Core building block for"
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZINWINDOW instead of comparing TZPART results manually?","answer":"Use TZINWINDOW when you need to check whether a moment falls within a time-of-day range, especially when you also want to restrict to certain days of the week — it handles both checks in a single call."},{"question":"What does the days_mask parameter do, and what happens if I omit it?","answer":"days_mask is a 7-character string 'SMTWTFS' where _ excludes that weekday; omitting it means all days are included, so only the time-of-day window is checked."},{"question":"What happens if start_local is greater than end_local?","answer":"TZINWINDOW evaluates the interval as given — since it checks local time-of-day against [start_local, end_local), passing a start that is larger than end will result in FALSE for most times because the range is effectively empty or wraps unexpectedly."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZINWINDOW(TZDATETIME(2025, 3, 15, 14, 30, 0, \"America/New_York\"), 9/24, 17/24, \"_MTWTF_\")"} />

## When to use it

### When should I use TZINWINDOW instead of comparing TZPART results manually?

Use TZINWINDOW when you need to check whether a moment falls within a time-of-day range, especially when you also want to restrict to certain days of the week — it handles both checks in a single call.

### What does the days_mask parameter do, and what happens if I omit it?

days_mask is a 7-character string 'SMTWTFS' where _ excludes that weekday; omitting it means all days are included, so only the time-of-day window is checked.

### What happens if start_local is greater than end_local?

TZINWINDOW evaluates the interval as given — since it checks local time-of-day against [start_local, end_local), passing a start that is larger than end will result in FALSE for most times because the range is effectively empty or wraps unexpectedly.

## Use cases

**Check if a shipment timestamp is within a warehouse receiving window**

<TryIt formula={"=TZINWINDOW(TZDATETIME(2025, 6, 10, 7, 45, 0, \"America/Chicago\"), 6/24, 10/24, \"_MTWTF_\")"} />

**Determine if a trade timestamp falls within market open hours**

<TryIt formula={"=TZINWINDOW(TZDATETIME(2025, 1, 22, 15, 0, 0, \"America/New_York\"), 9.5/24, 16/24, \"_MTWTF_\")"} />

## Used in

[operations](/industries/operations) · [logistics](/industries/logistics) · [finance](/industries/finance)

## Syntax

```
TZINWINDOW(zoned,start_local,end_local,[days_mask])
```

