---
title: "BYROW"
description: "Applies a LAMBDA to each row of an array"
seoTitle: "BYROW: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `BYROW` — conformant with Google Sheets and Excel. Applies a LAMBDA to each row of an array"
llmsDescription: "TrueCalc BYROW function: Applies a LAMBDA to each row of an array Google Sheets/Excel conformant with fixture-verified behavior."
category: "array"
jsonLdFaq: [{"question":"When should I use BYROW?","answer":"Use BYROW to apply a LAMBDA function to each row of an array individually, returning a single aggregated result per row."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=BYROW({1,2;3,4},LAMBDA(row,SUM(row)))"} />

## When to use it

### When should I use BYROW?

Use BYROW to apply a LAMBDA function to each row of an array individually, returning a single aggregated result per row.

## Use cases

**Sum each row of a 2x2 matrix**

<TryIt formula={"=BYROW({1,2;3,4},LAMBDA(row,SUM(row)))"} />

## Used in

[operations](/industries/operations) · [science](/industries/science)

## Syntax

```
BYROW(array, lambda)
```

## Examples

```formula
=BYROW({1,2;3,4},LAMBDA(row,SUM(row))) // => 3
=BYROW({1,2;3,4;5,6},LAMBDA(row,AVERAGE(row))) // => 1.5
=BYROW({3,1,5;4,2,6},LAMBDA(row,MAX(row))) // => 5
```

