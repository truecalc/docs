---
title: "TZNOW"
description: "The current moment stamped with the given zone (volatile; pinned during recalc)"
seoTitle: "TZNOW: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZNOW` — conformant with Google Sheets and Excel. The current moment stamped with the given zone (volatile; pinned during recalc)"
llmsDescription: "Use TZNOW(zone) to get the current moment as a zone-aware instant in the given IANA zone. Volatile — recalculates each pass — but pinned within a single recalc so all cells see the same \"now\". Pass to TZCONVERT, TZDIFF, or TZINWINDOW for business-hou"
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZNOW instead of NOW()?","answer":"Use TZNOW when you need a zone-aware timestamp that carries its timezone with it. NOW() returns a plain serial number with no zone context, while TZNOW returns a true instant tied to a specific IANA zone, preventing ambiguity across regions."},{"question":"Will different cells calling TZNOW get different times in the same recalc?","answer":"No. TZNOW pins the clock at recalc-start, so every cell that calls TZNOW during a single recalc pass sees the identical instant, ensuring consistency across your workbook."},{"question":"What happens if I pass an invalid IANA zone to TZNOW?","answer":"TZNOW returns an error if the zone string is not a valid IANA timezone identifier. Always use canonical names like \"America/New_York\" or \"Europe/London\" rather than abbreviations like \"EST\"."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZSTRING(TZDATETIME(2026, 6, 23, 9, 0, 0, \"America/Chicago\"))"} />

## When to use it

### When should I use TZNOW instead of NOW()?

Use TZNOW when you need a zone-aware timestamp that carries its timezone with it. NOW() returns a plain serial number with no zone context, while TZNOW returns a true instant tied to a specific IANA zone, preventing ambiguity across regions.

### Will different cells calling TZNOW get different times in the same recalc?

No. TZNOW pins the clock at recalc-start, so every cell that calls TZNOW during a single recalc pass sees the identical instant, ensuring consistency across your workbook.

### What happens if I pass an invalid IANA zone to TZNOW?

TZNOW returns an error if the zone string is not a valid IANA timezone identifier. Always use canonical names like "America/New_York" or "Europe/London" rather than abbreviations like "EST".

## Use cases

**Convert a UTC instant to Tokyo local time**

<TryIt formula={"=TZSTRING(TZCONVERT(TZDATETIME(2026, 6, 23, 9, 0, 0, \"UTC\"), \"Asia/Tokyo\"))"} />

**Check whether a moment falls within business hours**

<TryIt formula={"=TZINWINDOW(TZDATETIME(2026, 6, 23, 10, 30, 0, \"Europe/London\"), 9/24, 17/24)"} />

## Used in

[operations](/industries/operations) · [engineering](/industries/engineering) · [logistics](/industries/logistics)

## Syntax

```
TZNOW(zone)
```

