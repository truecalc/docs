---
title: "ISZONED"
description: "TRUE if the value is a zone-aware instant"
seoTitle: "ISZONED: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `ISZONED` — conformant with Google Sheets and Excel. TRUE if the value is a zone-aware instant"
llmsDescription: "TrueCalc ISZONED function: TRUE if the value is a zone-aware instant Google Sheets/Excel conformant with fixture-verified behavior."
category: "timezone"
jsonLdFaq: [{"question":"When should I use ISZONED instead of just calling a timezone function directly?","answer":"Use ISZONED to guard a formula that may receive either a plain date serial or a zoned instant, so you can branch logic and avoid passing an unzoned value to a function like TZCONVERT that requires a zoned input."},{"question":"What does ISZONED return for a plain number or a text string?","answer":"ISZONED returns FALSE for plain numbers, text, dates created without a timezone, and errors — only values produced by TZDATETIME, TZCONVERT, or similar zone-aware functions return TRUE."},{"question":"Can I use ISZONED to validate user-provided values before further timezone calculations?","answer":"Yes. Wrap ISZONED in an IF to detect whether a value is zone-aware and fall back to a safe default or localization step when it is not."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=ISZONED(TZDATETIME(2024, 3, 10, 9, 0, 0, \\\"America/New_York\\\"))"} />

## When to use it

### When should I use ISZONED instead of just calling a timezone function directly?

Use ISZONED to guard a formula that may receive either a plain date serial or a zoned instant, so you can branch logic and avoid passing an unzoned value to a function like TZCONVERT that requires a zoned input.

### What does ISZONED return for a plain number or a text string?

ISZONED returns FALSE for plain numbers, text, dates created without a timezone, and errors — only values produced by TZDATETIME, TZCONVERT, or similar zone-aware functions return TRUE.

### Can I use ISZONED to validate user-provided values before further timezone calculations?

Yes. Wrap ISZONED in an IF to detect whether a value is zone-aware and fall back to a safe default or localization step when it is not.

## Use cases

**Branch logic based on whether a value is zoned**

<TryIt formula={"=IF(ISZONED(TZDATETIME(2024, 6, 15, 14, 30, 0, \\\"Europe/London\\\")), \\\"zoned\\\", \\\"plain\\\")"} />

**Confirm a localized instant is recognized as zoned**

<TryIt formula={"=ISZONED(TZLOCALIZE(45000, \\\"Asia/Tokyo\\\"))"} />

## Used in

[engineering](/industries/engineering) · [operations](/industries/operations) · [finance](/industries/finance)

## Syntax

```
ISZONED(value)
```

