---
title: "TZOVERLAP"
description: "First daily window when the local time is within [start,end) in ALL zones; [start,end] instants or 'No overlap'"
seoTitle: "TZOVERLAP: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZOVERLAP` — conformant with Google Sheets and Excel. First daily window when the local time is within [start,end) in ALL zones; [start,end] instants "
llmsDescription: "TrueCalc TZOVERLAP function: First daily window when the local time is within [start,end) in ALL zones; [start,end] instants or 'No overlap' Google Sheets/Excel conformant with fixture-verified behavi"
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZOVERLAP instead of checking time zones manually?","answer":"Use TZOVERLAP when you need to find a single window where all participants across multiple time zones are simultaneously within business hours — it handles DST and date-line edge cases automatically."},{"question":"What does TZOVERLAP return when no common window exists?","answer":"It returns the string \\\"No overlap\\\" when there is no moment in the given day where all listed zones simultaneously fall within the specified local time range."},{"question":"How does the optional granularity_min parameter affect the result?","answer":"It controls the step size used when scanning the day for an overlap window; a smaller value (e.g., 5 minutes) gives a more precise start instant at the cost of more computation, while the default is typically 30 minutes."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZOVERLAP({\\\"America/New_York\\\",\\\"Europe/Berlin\\\",\\\"Asia/Tokyo\\\"}, 9, 18, DATE(2024, 9, 18))"} />

## When to use it

### When should I use TZOVERLAP instead of checking time zones manually?

Use TZOVERLAP when you need to find a single window where all participants across multiple time zones are simultaneously within business hours — it handles DST and date-line edge cases automatically.

### What does TZOVERLAP return when no common window exists?

It returns the string \"No overlap\" when there is no moment in the given day where all listed zones simultaneously fall within the specified local time range.

### How does the optional granularity_min parameter affect the result?

It controls the step size used when scanning the day for an overlap window; a smaller value (e.g., 5 minutes) gives a more precise start instant at the cost of more computation, while the default is typically 30 minutes.

## Use cases

**Locate an overlap for a US-UK logistics call within standard hours**

<TryIt formula={"=TZOVERLAP({\\\"America/Chicago\\\",\\\"Europe/London\\\"}, 8, 17, DATE(2024, 11, 5))"} />

**Check for a narrow afternoon overlap with 15-minute precision**

<TryIt formula={"=TZOVERLAP({\\\"America/Los_Angeles\\\",\\\"Asia/Singapore\\\"}, 14, 18, DATE(2024, 6, 10), 15)"} />

## Used in

[hr](/industries/hr) · [operations](/industries/operations) · [logistics](/industries/logistics)

## Syntax

```
TZOVERLAP(zones,start_local,end_local,date_serial,[granularity_min])
```

