---
title: "TZCONVERT"
description: "Same instant shown as the local time in another zone"
seoTitle: "TZCONVERT: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `TZCONVERT` — conformant with Google Sheets and Excel. Same instant shown as the local time in another zone"
llmsDescription: "Use TZCONVERT(zoned,target_zone) to view the same instant in a different IANA timezone — the underlying UTC moment is unchanged, only the local clock display shifts. The core \"what time is it there?\" function. Compose with TZDATETIME or TZNOW as inpu"
category: "timezone"
jsonLdFaq: [{"question":"When should I use TZCONVERT instead of adding a fixed offset manually?","answer":"Use TZCONVERT whenever you need to account for daylight saving time. A fixed offset like +1 or -5 breaks twice a year; TZCONVERT uses IANA zone rules so the result is always correct regardless of DST transitions."},{"question":"What happens if the target zone observes daylight saving time on that date?","answer":"TZCONVERT automatically applies the correct offset for the exact instant, so the output reflects DST if it is in effect — no manual adjustment is needed."},{"question":"When should I use TZCONVERT versus TZDIFF?","answer":"Use TZCONVERT when you need the actual local time in another zone. Use TZDIFF when you only need the numeric hour difference between two zones."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=TZCONVERT(TZDATETIME(2024, 3, 15, 9, 0, 0, \"America/New_York\"), \"Europe/Berlin\")"} />

## When to use it

### When should I use TZCONVERT instead of adding a fixed offset manually?

Use TZCONVERT whenever you need to account for daylight saving time. A fixed offset like +1 or -5 breaks twice a year; TZCONVERT uses IANA zone rules so the result is always correct regardless of DST transitions.

### What happens if the target zone observes daylight saving time on that date?

TZCONVERT automatically applies the correct offset for the exact instant, so the output reflects DST if it is in effect — no manual adjustment is needed.

### When should I use TZCONVERT versus TZDIFF?

Use TZCONVERT when you need the actual local time in another zone. Use TZDIFF when you only need the numeric hour difference between two zones.

## Use cases

**Show a Tokyo market open in London time**

<TryIt formula={"=TZCONVERT(TZDATETIME(2024, 6, 1, 9, 0, 0, \"Asia/Tokyo\"), \"Europe/London\")"} />

**Convert a UTC timestamp to a customer's local zone**

<TryIt formula={"=TZCONVERT(TZDATETIME(2024, 11, 5, 14, 30, 0, \"UTC\"), \"America/Los_Angeles\")"} />

## Used in

[operations](/industries/operations) · [hr](/industries/hr) · [finance](/industries/finance)

## Syntax

```
TZCONVERT(zoned,target_zone)
```

