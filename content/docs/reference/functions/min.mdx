---
title: "MIN"
description: "Minimum value"
seoTitle: "MIN: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `MIN` — conformant with Google Sheets and Excel. Minimum value"
llmsDescription: "TrueCalc MIN function: Minimum value Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use MIN instead of SMALL?","answer":"Use MIN for the single lowest value; use SMALL when you need the second-lowest, third-lowest, and so on."},{"question":"Does MIN ignore text and blanks?","answer":"Yes. MIN considers only numeric values and skips text and empty cells."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=MIN(3, 7, 2)"} />

## When to use it

### When should I use MIN instead of SMALL?

Use MIN for the single lowest value; use SMALL when you need the second-lowest, third-lowest, and so on.

### Does MIN ignore text and blanks?

Yes. MIN considers only numeric values and skips text and empty cells.

## Use cases

**Get the smallest value from a list**

<TryIt formula={"=MIN({12, 45, 23, 8})"} />

**Identify the lowest quoted price**

<TryIt formula={"=MIN(1500, 2300, 1800, 2100)"} />

## Used in

[operations](/industries/operations) · [science](/industries/science)

## Syntax

```
MIN(value1,...)
```

## Examples

```formula
=MIN({4,26,2,14,5}) // => 2
=MIN({1,2,3,4,5},26,4) // => 1
=MIN({-3,0,5,10}) // => -3
```

