---
title: "SECOND"
description: "Second component of a time serial number"
seoTitle: "SECOND: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `SECOND` — conformant with Google Sheets and Excel. Second component of a time serial number"
llmsDescription: "TrueCalc SECOND function: Second component of a time serial number Google Sheets/Excel conformant with fixture-verified behavior."
category: "date"
jsonLdFaq: [{"question":"When should I use SECOND?","answer":"Use SECOND to extract the seconds component (0–59) from a time serial number or time string, useful when you need sub-minute precision from a timestamp."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=SECOND(TIME(11,40,59))"} />

## When to use it

### When should I use SECOND?

Use SECOND to extract the seconds component (0–59) from a time serial number or time string, useful when you need sub-minute precision from a timestamp.

## Use cases

**Extract the seconds from a constructed time**

<TryIt formula={"=SECOND(TIME(11,40,59))"} />

## Used in

[operations](/industries/operations) · [engineering](/industries/engineering)

## Syntax

```
SECOND(time)
```

## Examples

```formula
=SECOND(TIME(11,40,59)) // => 59
=SECOND(40909.0004) // => 35
=SECOND("20:49:59") // => 59
```

