---
title: "COUPDAYBS"
description: "Days from coupon start to settlement"
seoTitle: "COUPDAYBS: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `COUPDAYBS` — conformant with Google Sheets and Excel. Days from coupon start to settlement"
llmsDescription: "TrueCalc COUPDAYBS function: Days from coupon start to settlement Google Sheets/Excel conformant with fixture-verified behavior."
category: "financial"
jsonLdFaq: [{"question":"When should I use COUPDAYBS?","answer":"Use COUPDAYBS to find the number of days from the start of the current coupon period to the settlement date of a bond — essential for accrued-interest calculations."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=COUPDAYBS(DATE(2010,2,1),DATE(2019,12,31),4)"} />

## When to use it

### When should I use COUPDAYBS?

Use COUPDAYBS to find the number of days from the start of the current coupon period to the settlement date of a bond — essential for accrued-interest calculations.

## Use cases

**Days accrued in current coupon period**

<TryIt formula={"=COUPDAYBS(DATE(2010,2,1),DATE(2019,12,31),4)"} />

## Used in

[finance](/industries/finance)

## Syntax

```
COUPDAYBS(settlement, maturity, frequency, [basis])
```

## Examples

```formula
=COUPDAYBS(DATE(2010,2,1),DATE(2019,12,31),4) // => 31
=COUPDAYBS(DATE(2010,2,1),DATE(2019,12,31),4,1) // => 32
```

