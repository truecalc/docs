---
title: "CHOOSE"
description: "Returns the value at the given 1-based index"
seoTitle: "CHOOSE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `CHOOSE` — conformant with Google Sheets and Excel. Returns the value at the given 1-based index"
llmsDescription: "TrueCalc CHOOSE function: Returns the value at the given 1-based index Google Sheets/Excel conformant with fixture-verified behavior."
category: "lookup"
jsonLdFaq: [{"question":"When should I use CHOOSE instead of LOOKUP?","answer":"Use CHOOSE to pick one value from a fixed list by its position number; use LOOKUP to search a sorted vector for a matching value."},{"question":"Can CHOOSE return text or only numbers?","answer":"CHOOSE can return any value type, including text, numbers, or the result of another formula, based on the index you pass."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=CHOOSE(2, \"a\", \"b\", \"c\")"} />

## When to use it

### When should I use CHOOSE instead of LOOKUP?

Use CHOOSE to pick one value from a fixed list by its position number; use LOOKUP to search a sorted vector for a matching value.

### Can CHOOSE return text or only numbers?

CHOOSE can return any value type, including text, numbers, or the result of another formula, based on the index you pass.

## Use cases

**Convert a month number to a quarter label**

```formula
=CHOOSE(7, "Q1", "Q1", "Q1", "Q2", "Q2", "Q2", "Q3", "Q3", "Q3", "Q4", "Q4", "Q4")
```

**Select a commission rate by tier number**

<TryIt formula={"=CHOOSE(3, 0.02, 0.04, 0.06)"} />

## Used in

[operations](/industries/operations) · [sales](/industries/sales)

## Syntax

```
CHOOSE(index, value1, value2, ...)
```

## Examples

```formula
=CHOOSE(2,10,20,30) // => 20
=CHOOSE(3,100,200,300) // => 300
=CHOOSE(1,10,20,30) // => 10
```

