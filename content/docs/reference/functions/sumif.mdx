---
title: "SUMIF"
description: "Sum cells where range matches criterion"
seoTitle: "SUMIF: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `SUMIF` — conformant with Google Sheets and Excel. Sum cells where range matches criterion"
llmsDescription: "TrueCalc SUMIF function: Sum cells where range matches criterion Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use SUMIF instead of SUM?","answer":"Use SUMIF to total only the cells that meet a condition; use plain SUM when you want to add everything."},{"question":"When should I use SUMIF instead of COUNTIF?","answer":"Use SUMIF to add up the values that match a condition; use COUNTIF when you only want the number of matching cells."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=SUMIF({1, 2, 3, 4}, \">2\")"} />

## When to use it

### When should I use SUMIF instead of SUM?

Use SUMIF to total only the cells that meet a condition; use plain SUM when you want to add everything.

### When should I use SUMIF instead of COUNTIF?

Use SUMIF to add up the values that match a condition; use COUNTIF when you only want the number of matching cells.

## Use cases

**Sum amounts that meet a minimum**

<TryIt formula={"=SUMIF({100, 250, 75, 400}, \">=200\")"} />

**Total values equal to a target**

<TryIt formula={"=SUMIF({5, 10, 5, 20}, 5)"} />

## Used in

[operations](/industries/operations)

## Syntax

```
SUMIF(range, criterion, [sum_range])
```

## Examples

```formula
=SUMIF({10,20,30,40},">20") // => 70
```

