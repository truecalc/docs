---
title: "MROUND"
description: "Round to nearest multiple"
seoTitle: "MROUND: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `MROUND` — conformant with Google Sheets and Excel. Round to nearest multiple"
llmsDescription: "TrueCalc MROUND function: Round to nearest multiple Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use MROUND instead of ROUND?","answer":"Use MROUND when you need to round to a specific multiple rather than a number of decimal places — for example rounding prices to the nearest 0.05 or time values to the nearest 15 minutes."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=MROUND(21,14)"} />

## When to use it

### When should I use MROUND instead of ROUND?

Use MROUND when you need to round to a specific multiple rather than a number of decimal places — for example rounding prices to the nearest 0.05 or time values to the nearest 15 minutes.

## Use cases

**Round a quantity to the nearest case size of 14 units**

<TryIt formula={"=MROUND(21,14)"} />

## Used in

[operations](/industries/operations) · [retail](/industries/retail)

## Syntax

```
MROUND(number, multiple)
```

## Examples

```formula
=MROUND(21,14) // => 28
=MROUND(10,3) // => 9
```

