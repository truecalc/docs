---
title: "MAXIFS"
description: "Maximum with multiple criteria"
seoTitle: "MAXIFS: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `MAXIFS` — conformant with Google Sheets and Excel. Maximum with multiple criteria"
llmsDescription: "TrueCalc MAXIFS function: Maximum with multiple criteria Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use MAXIFS instead of MAX?","answer":"Use MAXIFS to find the largest value among only the rows that meet one or more conditions, such as the highest sale in the West region this quarter. Plain MAX ignores conditions and looks at every value."},{"question":"What does MAXIFS return when no rows meet the conditions?","answer":"If no rows satisfy all the criteria, MAXIFS returns 0 rather than an error, so check your conditions if you unexpectedly see 0."},{"question":"What is the argument order for MAXIFS?","answer":"It starts with the range to take the maximum of, then pairs of criteria range and criterion: MAXIFS(max_range, criteria_range1, criterion1, criteria_range2, criterion2, ...). MINIFS follows the same pattern for the smallest value."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

## When to use it

### When should I use MAXIFS instead of MAX?

Use MAXIFS to find the largest value among only the rows that meet one or more conditions, such as the highest sale in the West region this quarter. Plain MAX ignores conditions and looks at every value.

### What does MAXIFS return when no rows meet the conditions?

If no rows satisfy all the criteria, MAXIFS returns 0 rather than an error, so check your conditions if you unexpectedly see 0.

### What is the argument order for MAXIFS?

It starts with the range to take the maximum of, then pairs of criteria range and criterion: MAXIFS(max_range, criteria_range1, criterion1, criteria_range2, criterion2, ...). MINIFS follows the same pattern for the smallest value.

## Used in

[sales](/industries/sales) · [finance](/industries/finance) · [operations](/industries/operations) · [retail](/industries/retail) · [hr](/industries/hr)

## Syntax

```
MAXIFS(max_range,criteria_range1,criteria1,...)
```

