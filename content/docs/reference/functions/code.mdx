---
title: "CODE"
description: "Numeric code of first character"
seoTitle: "CODE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `CODE` — conformant with Google Sheets and Excel. Numeric code of first character"
llmsDescription: "TrueCalc CODE function: Numeric code of first character Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use CODE?","answer":"Use CODE when you need the numeric character code of the first character in a string — for example, to detect whether a cell starts with an uppercase letter, a digit, or a special character, or to build character-range validation logic."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=CODE(\"A\")"} />

## When to use it

### When should I use CODE?

Use CODE when you need the numeric character code of the first character in a string — for example, to detect whether a cell starts with an uppercase letter, a digit, or a special character, or to build character-range validation logic.

## Used in

[operations](/industries/operations)

## Syntax

```
CODE(text)
```

## Examples

```formula
=CODE("a") // => 97
=CODE("A") // => 65
=CODE("ABC") // => 65
```

