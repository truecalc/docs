---
title: "COLUMN"
description: "Returns the column number of a cell reference"
seoTitle: "COLUMN: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `COLUMN` — conformant with Google Sheets and Excel. Returns the column number of a cell reference"
llmsDescription: "TrueCalc COLUMN function: Returns the column number of a cell reference Google Sheets/Excel conformant with fixture-verified behavior."
category: "lookup"
jsonLdFaq: [{"question":"When should I use COLUMN?","answer":"Use COLUMN to obtain the column number of a cell reference — useful when building dynamic array formulas that need to offset or iterate by column position."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

## When to use it

### When should I use COLUMN?

Use COLUMN to obtain the column number of a cell reference — useful when building dynamic array formulas that need to offset or iterate by column position.

## Used in

[operations](/industries/operations) · [finance](/industries/finance)

## Syntax

```
COLUMN([cell_ref])
```

## Examples

```formula
=COLUMN(INDIRECT("A1")) // => 1
=COLUMN(INDIRECT("B1")) // => 2
=COLUMN(INDIRECT("C5")) // => 3
```

