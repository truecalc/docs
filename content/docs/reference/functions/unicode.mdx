---
title: "UNICODE"
description: "Unicode code point of first character"
seoTitle: "UNICODE: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `UNICODE` — conformant with Google Sheets and Excel. Unicode code point of first character"
llmsDescription: "TrueCalc UNICODE function: Unicode code point of first character Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use UNICODE instead of CODE?","answer":"Use UNICODE when you need the full Unicode code point of the first character, including characters outside the basic ASCII range such as accented letters, CJK characters, or emoji."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=UNICODE(\"Alphabet\")"} />

## When to use it

### When should I use UNICODE instead of CODE?

Use UNICODE when you need the full Unicode code point of the first character, including characters outside the basic ASCII range such as accented letters, CJK characters, or emoji.

## Use cases

**Get the Unicode code point of the first character**

<TryIt formula={"=UNICODE(\"Alphabet\")"} />

## Used in

[operations](/industries/operations) · [education](/industries/education)

## Syntax

```
UNICODE(text)
```

## Examples

```formula
=UNICODE("Alphabet") // => 65
=UNICODE(123) // => 49
=UNICODE("😀😀😄") // => 128512
```

