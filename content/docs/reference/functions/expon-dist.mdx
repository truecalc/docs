---
title: "EXPON.DIST"
description: "Exponential distribution"
seoTitle: "EXPON.DIST: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `EXPON.DIST` — conformant with Google Sheets and Excel. Exponential distribution"
llmsDescription: "TrueCalc EXPON.DIST function: Exponential distribution Google Sheets/Excel conformant with fixture-verified behavior."
category: "statistical"
jsonLdFaq: [{"question":"When should I use EXPON.DIST?","answer":"Use EXPON.DIST to compute the exponential distribution PDF (cumulative=FALSE) or CDF (cumulative=TRUE) — modeling the time between events in a Poisson process, such as waiting times or equipment failure intervals."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=EXPON.DIST(4,0.5,TRUE)"} />

## When to use it

### When should I use EXPON.DIST?

Use EXPON.DIST to compute the exponential distribution PDF (cumulative=FALSE) or CDF (cumulative=TRUE) — modeling the time between events in a Poisson process, such as waiting times or equipment failure intervals.

## Used in

[operations](/industries/operations) · [science](/industries/science)

## Syntax

```
EXPON.DIST(x,lambda,cumulative)
```

## Examples

```formula
=EXPON.DIST(4,0.5,FALSE) // => 0.06766764161830635
=EXPON.DIST(4,0.5,TRUE) // => 0.8646647167633873
```

