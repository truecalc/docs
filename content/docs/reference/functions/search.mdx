---
title: "SEARCH"
description: "Case-insensitive search with wildcards"
seoTitle: "SEARCH: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `SEARCH` — conformant with Google Sheets and Excel. Case-insensitive search with wildcards"
llmsDescription: "TrueCalc SEARCH function: Case-insensitive search with wildcards Google Sheets/Excel conformant with fixture-verified behavior."
category: "text"
jsonLdFaq: [{"question":"When should I use SEARCH instead of FIND?","answer":"Use SEARCH when you want a case-insensitive match or wildcards; use FIND when the match must respect upper and lower case exactly."},{"question":"Is SEARCH case-sensitive?","answer":"No — SEARCH ignores case, so searching for B will match either b or B in the text."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=SEARCH(\"B\",\"aBc\")"} />

## When to use it

### When should I use SEARCH instead of FIND?

Use SEARCH when you want a case-insensitive match or wildcards; use FIND when the match must respect upper and lower case exactly.

### Is SEARCH case-sensitive?

No — SEARCH ignores case, so searching for B will match either b or B in the text.

## Use cases

**Locate a keyword inside an address**

<TryIt formula={"=SEARCH(\"street\",\"123 Main Street\")"} />

**Detect a term in a log line regardless of case**

<TryIt formula={"=SEARCH(\"error\",\"System Error log\")"} />

## Used in

[operations](/industries/operations) · [sales](/industries/sales)

## Syntax

```
SEARCH(find_text, within_text, [start_num])
```

## Examples

```formula
=SEARCH("n","printing") // => 4
=SEARCH("N","printing") // => 4
=SEARCH("a","banana",3) // => 4
```

