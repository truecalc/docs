---
title: "QUOTIENT"
description: "Integer portion of division"
seoTitle: "QUOTIENT: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `QUOTIENT` — conformant with Google Sheets and Excel. Integer portion of division"
llmsDescription: "TrueCalc QUOTIENT function: Integer portion of division Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use QUOTIENT instead of regular division?","answer":"Use QUOTIENT when you need only the whole-number part of a division — for example calculating how many full boxes fit into a shipment, discarding any remainder."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=QUOTIENT(9,2)"} />

## When to use it

### When should I use QUOTIENT instead of regular division?

Use QUOTIENT when you need only the whole-number part of a division — for example calculating how many full boxes fit into a shipment, discarding any remainder.

## Use cases

**How many full pairs can be made from 9 items**

<TryIt formula={"=QUOTIENT(9,2)"} />

## Used in

[operations](/industries/operations) · [retail](/industries/retail)

## Syntax

```
QUOTIENT(numerator, denominator)
```

## Examples

```formula
=QUOTIENT(4,2) // => 2
```

