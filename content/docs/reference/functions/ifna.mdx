---
title: "IFNA"
description: "Return alternate value on #N/A"
seoTitle: "IFNA: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `IFNA` — conformant with Google Sheets and Excel. Return alternate value on #N/A"
llmsDescription: "TrueCalc IFNA function: Return alternate value on #N/A Google Sheets/Excel conformant with fixture-verified behavior."
category: "logical"
jsonLdFaq: [{"question":"When should I use IFNA instead of IFERROR?","answer":"Use IFNA to catch only the #N/A error, which usually means a lookup failed; use IFERROR when you want to catch every kind of error."},{"question":"Why prefer IFNA over IFERROR for lookups?","answer":"IFNA leaves other errors like #DIV/0! visible so real mistakes are not hidden, while still replacing the #N/A from a failed lookup."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=IFNA(VLOOKUP(\"z\",{\"a\",1;\"b\",2},2,FALSE),\"Not found\")"} />

## When to use it

### When should I use IFNA instead of IFERROR?

Use IFNA to catch only the #N/A error, which usually means a lookup failed; use IFERROR when you want to catch every kind of error.

### Why prefer IFNA over IFERROR for lookups?

IFNA leaves other errors like #DIV/0! visible so real mistakes are not hidden, while still replacing the #N/A from a failed lookup.

## Use cases

**Show a friendly message when a lookup misses**

<TryIt formula={"=IFNA(VLOOKUP(\"z\",{\"a\",1;\"b\",2},2,FALSE),\"Not found\")"} />

**Replace an #N/A with a fallback word**

<TryIt formula={"=IFNA(NA(),\"fallback\")"} />

**Default a missing lookup to zero**

<TryIt formula={"=IFNA(NA(),0)"} />

## Used in

[sales](/industries/sales) · [retail](/industries/retail) · [operations](/industries/operations) · [finance](/industries/finance) · [education](/industries/education)

## Syntax

```
IFNA(value, value_if_na)
```

## Examples

```formula
=IFNA(205,"Na error") // => 205
=IFNA(2+3,0) // => 5
```

