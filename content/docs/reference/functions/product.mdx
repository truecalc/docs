---
title: "PRODUCT"
description: "Product of arguments"
seoTitle: "PRODUCT: Spreadsheet Function in Google Sheets & Excel"
seoDescription: "TrueCalc `PRODUCT` — conformant with Google Sheets and Excel. Product of arguments"
llmsDescription: "TrueCalc PRODUCT function: Product of arguments Google Sheets/Excel conformant with fixture-verified behavior."
category: "math"
jsonLdFaq: [{"question":"When should I use PRODUCT instead of just typing * between values?","answer":"Use PRODUCT when you want to multiply many values or a whole range at once; the * operator is clearer for a couple of fixed numbers, but PRODUCT scales to lists and arrays without repeating the operator."},{"question":"Does PRODUCT ignore text and blank cells?","answer":"Yes. PRODUCT multiplies only the numeric values it finds and skips text and empty cells, so stray labels in a range will not break the calculation."}]
---

{/* GENERATED by scripts/gen-docs.mjs from functions.json + enrichment/ — do not edit. */}

import { TryIt } from '@/components/try-it';

## Try it

<TryIt formula={"=PRODUCT(2, 3, 4)"} />

## When to use it

### When should I use PRODUCT instead of just typing * between values?

Use PRODUCT when you want to multiply many values or a whole range at once; the * operator is clearer for a couple of fixed numbers, but PRODUCT scales to lists and arrays without repeating the operator.

### Does PRODUCT ignore text and blank cells?

Yes. PRODUCT multiplies only the numeric values it finds and skips text and empty cells, so stray labels in a range will not break the calculation.

## Use cases

**Multiply several factors together**

<TryIt formula={"=PRODUCT(2, 3, 4)"} />

**Apply a markup to a base price**

<TryIt formula={"=PRODUCT(150, 1.08)"} />

**Compound a series of growth factors**

<TryIt formula={"=PRODUCT({1.05, 1.03, 1.07, 1.02})"} />

## Used in

[finance](/industries/finance) · [operations](/industries/operations) · [retail](/industries/retail) · [engineering](/industries/engineering)

## Syntax

```
PRODUCT(value1,...)
```

## Examples

```formula
=PRODUCT(5) // => 5
=PRODUCT(1,2,3,4,5) // => 120
```

