---
title: MCP server
description: TrueCalc tools for MCP clients such as Claude Desktop and Claude Code.
---

Stub — this section will be generated from the `truecalc-mcp` tool schemas
(`evaluate`, `validate`, `explain`, `batch_evaluate`, `list_functions`, and the
upcoming `workbook_*` tools).
