when_to_use:
  - q: "When should I use HSTACK?"
    a: "Use HSTACK to combine two or more arrays side by side into a single wider array, useful for assembling columns from different sources."
use_cases:
  - title: "Join two arrays horizontally"
    formula: "=HSTACK({1,2},{3,4})"
    industries: [operations, science]
industries: [operations, science]
related: [VSTACK, CHOOSECOLS, ARRAY_CONSTRAIN]
