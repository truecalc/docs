when_to_use:
  - q: "When should I use RANDARRAY instead of RAND?"
    a: "Use RANDARRAY when you need a whole grid of random numbers at once — for example populating a matrix for a simulation or generating multiple test samples in a single formula."
industries: [science, education]
related: [RAND, RANDBETWEEN]
