when_to_use:
  - q: "When should I use BIN2DEC?"
    a: "Use BIN2DEC to convert a binary (base-2) string to its decimal integer equivalent, for example when decoding bit-field values or permissions stored as binary strings."
use_cases:
  - title: "Convert a binary string to decimal"
    formula: "=BIN2DEC("1100100")"
    industries: [engineering, science]
industries: [engineering, science]
related: [BIN2HEX, BIN2OCT, DEC2BIN, HEX2DEC]
